/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
  '/api/health': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content?: never;
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/sesion': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Usuario actual
     * @description Devuelve el usuario autenticado, su empresa activa y sus permisos.
     */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Usuario actualmente autenticado, su empresa activa y sus permisos. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario en sesión. */
              id: string;
              /** @description Nombre de inicio de sesión (minúsculas). */
              username: string;
              /** @description Nombre para mostrar del usuario. */
              nombre: string;
              /** @description Empresa activa de la sesión (multi-empresa explícito). */
              empresaActiva: {
                /** @description Id de la empresa activa de la sesión. */
                id: number;
                /** @description Nombre de la empresa activa. */
                nombre: string;
              };
              /** @description Claves de permiso efectivas del usuario (unión de sus roles). */
              permisos: (
                | 'ordenes.modificar'
                | 'ordenes.precio-maquila'
                | 'ordenes.habilitacion'
                | 'ordenes.ver-costos'
                | 'ordenes.ver-precio-real-maquila'
                | 'pedidos.modificar'
                | 'pedidos.modificar-reales'
                | 'pedidos.ver'
                | 'pedidos.administrar'
                | 'pedidos.importes'
                | 'pedidos-reales.administrar'
                | 'ordenes.ver'
                | 'ordenes.administrar'
                | 'ordenes.cancelar'
                | 'clientes.modificar'
                | 'proveedores.modificar'
                | 'etiquetas.modificar'
                | 'compras.autorizar'
                | 'produccion.corte-salidas'
                | 'produccion.entradas-maquila'
                | 'telas.ver-totales'
                | 'ipt.clasificar-modelos'
                | 'ipt.consultar-existencias'
                | 'ipt.fecha-libre'
                | 'ipt.modificar-movimientos'
                | 'ipt.cantidades-negativas'
                | 'esma.ver-pagos'
                | 'esma.modificar'
                | 'rc.ver-botones'
                | 'rc.fechas-retraso'
                | 'rc.fecha-libre-cumplimiento'
                | 'calidad.generar-auditorias'
                | 'calidad.modificar-auditorias'
                | 'calidad.actualizar-auditorias'
                | 'indicadores.fecha-libre'
                | 'indicadores.ip-productividad'
                | 'indicadores.ip-confiabilidad'
                | 'indicadores.ip-muestrarios'
                | 'indicadores.ciclicos-alta'
                | 'indicadores.ciclicos-conteo'
                | 'indicadores.ciclicos-consulta'
                | 'indicadores.almacen-productividad'
                | 'consultas.ver-importes'
                | 'usuarios.administrar'
                | 'roles.administrar'
                | 'almacenes.ver'
                | 'almacenes.administrar'
                | 'empresas.administrar'
                | 'proveedores.ver'
                | 'proveedores.administrar'
                | 'temporadas.ver'
                | 'temporadas.administrar'
                | 'etiquetas-marca.ver'
                | 'etiquetas-marca.administrar'
                | 'colores.ver'
                | 'colores.administrar'
                | 'tallas.ver'
                | 'tallas.administrar'
                | 'clientes.ver'
                | 'clientes.administrar'
                | 'telas.ver'
                | 'telas.administrar'
                | 'avios.ver'
                | 'avios.administrar'
                | 'bordados.ver'
                | 'bordados.administrar'
                | 'modelos.ver'
                | 'modelos.administrar'
                | 'tipos-proceso.ver'
                | 'tipos-proceso.administrar'
                | 'produccion.corte'
                | 'produccion.envio'
                | 'produccion.recibo'
                | 'produccion.entrega'
                | 'produccion.wip-ver'
                | 'produccion.cancelar'
                | 'inventario-pt.ver'
                | 'inventario-pt.mover'
                | 'esma.cargo-validar'
              )[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/almacenes': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar almacenes */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Filtra por tipo de almacén. */
          tipo?: 'PT' | 'TELA' | 'AVIO';
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Lista almacenes de todas las empresas ("true"/"false"). */
          todasLasEmpresas?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'nombre' | 'tipo' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de almacenes. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Almacenes de la página. */
              datos: {
                /** @description Id del almacén. */
                id: number;
                /** @description Nombre del almacén. */
                nombre: string;
                /**
                 * @description Tipo: PT, TELA o AVIO.
                 * @enum {string}
                 */
                tipo: 'PT' | 'TELA' | 'AVIO';
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /** @description Empresa dueña, o null si es un almacén global. */
                idEmpresa: number | null;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de almacenes que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un almacén */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            /** @enum {string} */
            tipo: 'PT' | 'TELA' | 'AVIO';
            idEmpresa?: number;
          };
        };
      };
      responses: {
        /** @description Almacén del catálogo (base del kardex único). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del almacén. */
              id: number;
              /** @description Nombre del almacén. */
              nombre: string;
              /**
               * @description Tipo: PT, TELA o AVIO.
               * @enum {string}
               */
              tipo: 'PT' | 'TELA' | 'AVIO';
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Empresa dueña, o null si es un almacén global. */
              idEmpresa: number | null;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/almacenes/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un almacén */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del almacén. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Almacén del catálogo (base del kardex único). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del almacén. */
              id: number;
              /** @description Nombre del almacén. */
              nombre: string;
              /**
               * @description Tipo: PT, TELA o AVIO.
               * @enum {string}
               */
              tipo: 'PT' | 'TELA' | 'AVIO';
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Empresa dueña, o null si es un almacén global. */
              idEmpresa: number | null;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un almacén (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del almacén. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Almacén del catálogo (base del kardex único). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del almacén. */
              id: number;
              /** @description Nombre del almacén. */
              nombre: string;
              /**
               * @description Tipo: PT, TELA o AVIO.
               * @enum {string}
               */
              tipo: 'PT' | 'TELA' | 'AVIO';
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Empresa dueña, o null si es un almacén global. */
              idEmpresa: number | null;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un almacén */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del almacén. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            /** @enum {string} */
            tipo?: 'PT' | 'TELA' | 'AVIO';
            idEmpresa?: number;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Almacén del catálogo (base del kardex único). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del almacén. */
              id: number;
              /** @description Nombre del almacén. */
              nombre: string;
              /**
               * @description Tipo: PT, TELA o AVIO.
               * @enum {string}
               */
              tipo: 'PT' | 'TELA' | 'AVIO';
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Empresa dueña, o null si es un almacén global. */
              idEmpresa: number | null;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/roles-proveedor': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar roles/servicios de proveedor (catálogo selector) */
    get: {
      parameters: {
        query?: {
          /** @description Incluye los roles desactivados ("true"/"false"). */
          incluirInactivos?: string;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del rol. */
              id: number;
              /** @description Clave estable del rol (kebab-case). */
              codigo: string;
              /** @description Nombre legible del rol. */
              nombre: string;
              /** @description Falso si está desactivado. */
              activo: boolean;
            }[];
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/proveedores': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar proveedores */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Filtra por tipo de proveedor. */
          tipo?: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
          /** @description Filtra por id de rol/servicio del proveedor (R15). */
          rol?: number;
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'nombre' | 'tipo' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de proveedores. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Proveedores de la página. */
              datos: {
                /** @description Id del proveedor. */
                id: number;
                /** @description Nombre del proveedor. */
                nombre: string;
                /** @description Razón social, o null. */
                razonSocial: string | null;
                /**
                 * @description Clasificación rápida: TELAS, AVIOS, SERVICIOS o SIN_CLASIFICAR.
                 * @enum {string}
                 */
                tipo: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
                /** @description Teléfono, o null. */
                telefono: string | null;
                /** @description Persona de contacto, o null. */
                contacto: string | null;
                /** @description Condiciones comerciales (texto libre), o null. */
                condiciones: string | null;
                /** @description ¿Emite CFDI? (formal/informal), o null. */
                factura: boolean | null;
                /** @description RFC, o null. */
                rfc: string | null;
                /** @description Régimen fiscal del SAT, o null. */
                regimenFiscalSat: string | null;
                /** @description Uso de CFDI habitual, o null. */
                usoCfdiHabitual: string | null;
                /** @description CP de expedición, o null. */
                codigoPostalExpedicion: string | null;
                /** @description ¿Se le retiene IVA?, o null. */
                retieneIva: boolean | null;
                /** @description ¿Se le retiene ISR?, o null. */
                retieneIsr: boolean | null;
                /** @description Email (para OC y XML), o null. */
                email: string | null;
                /** @description Dirección, o null. */
                direccion: string | null;
                /** @description Días de crédito (null/0 = contado). */
                diasCredito: number | null;
                /** @description Moneda habitual (MXN/USD), o null. */
                moneda: string | null;
                /** @description Forma de pago, o null. */
                formaPago: string | null;
                /** @description Método de pago CFDI (PUE/PPD), o null. */
                metodoPago: string | null;
                /** @description Banco, o null. */
                banco: string | null;
                /** @description CLABE interbancaria, o null. */
                clabe: string | null;
                /** @description Límite de crédito, o null. */
                limiteCredito: number | null;
                /** @description Lead time en días, o null. */
                leadTimeDias: number | null;
                /** @description Notas, o null. */
                notas: string | null;
                /** @description Código corto del taller (ex maquilero), o null. */
                corto: string | null;
                /** @description ¿Está asegurado? (talleres), o null. */
                asegurado: boolean | null;
                /** @description Observaciones de pago (talleres), o null. */
                obsPago: string | null;
                /** @description Roles/servicios del proveedor. */
                roles: {
                  /** @description Id del rol. */
                  id: number;
                  /** @description Clave estable del rol (kebab-case). */
                  codigo: string;
                  /** @description Nombre legible del rol. */
                  nombre: string;
                }[];
                /** @description Cantidad de adjuntos del proveedor. */
                cantidadAdjuntos: number;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de proveedores que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un proveedor */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            razonSocial?: string;
            /**
             * @default SIN_CLASIFICAR
             * @enum {string}
             */
            tipo?: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
            telefono?: string;
            contacto?: string;
            condiciones?: string;
            roles?: number[];
            factura?: boolean;
            rfc?: string;
            regimenFiscalSat?: string;
            usoCfdiHabitual?: string;
            codigoPostalExpedicion?: string;
            retieneIva?: boolean;
            retieneIsr?: boolean;
            /** Format: email */
            email?: string;
            direccion?: string;
            diasCredito?: number;
            /** @enum {string} */
            moneda?: 'MXN' | 'USD';
            formaPago?: string;
            /** @enum {string} */
            metodoPago?: 'PUE' | 'PPD';
            banco?: string;
            clabe?: string;
            limiteCredito?: number;
            leadTimeDias?: number;
            notas?: string;
            corto?: string;
            asegurado?: boolean;
            obsPago?: string;
          };
        };
      };
      responses: {
        /** @description Proveedor del catálogo (global, enriquecido R15). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del proveedor. */
              id: number;
              /** @description Nombre del proveedor. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /**
               * @description Clasificación rápida: TELAS, AVIOS, SERVICIOS o SIN_CLASIFICAR.
               * @enum {string}
               */
              tipo: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Condiciones comerciales (texto libre), o null. */
              condiciones: string | null;
              /** @description ¿Emite CFDI? (formal/informal), o null. */
              factura: boolean | null;
              /** @description RFC, o null. */
              rfc: string | null;
              /** @description Régimen fiscal del SAT, o null. */
              regimenFiscalSat: string | null;
              /** @description Uso de CFDI habitual, o null. */
              usoCfdiHabitual: string | null;
              /** @description CP de expedición, o null. */
              codigoPostalExpedicion: string | null;
              /** @description ¿Se le retiene IVA?, o null. */
              retieneIva: boolean | null;
              /** @description ¿Se le retiene ISR?, o null. */
              retieneIsr: boolean | null;
              /** @description Email (para OC y XML), o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Días de crédito (null/0 = contado). */
              diasCredito: number | null;
              /** @description Moneda habitual (MXN/USD), o null. */
              moneda: string | null;
              /** @description Forma de pago, o null. */
              formaPago: string | null;
              /** @description Método de pago CFDI (PUE/PPD), o null. */
              metodoPago: string | null;
              /** @description Banco, o null. */
              banco: string | null;
              /** @description CLABE interbancaria, o null. */
              clabe: string | null;
              /** @description Límite de crédito, o null. */
              limiteCredito: number | null;
              /** @description Lead time en días, o null. */
              leadTimeDias: number | null;
              /** @description Notas, o null. */
              notas: string | null;
              /** @description Código corto del taller (ex maquilero), o null. */
              corto: string | null;
              /** @description ¿Está asegurado? (talleres), o null. */
              asegurado: boolean | null;
              /** @description Observaciones de pago (talleres), o null. */
              obsPago: string | null;
              /** @description Roles/servicios del proveedor. */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Clave estable del rol (kebab-case). */
                codigo: string;
                /** @description Nombre legible del rol. */
                nombre: string;
              }[];
              /** @description Cantidad de adjuntos del proveedor. */
              cantidadAdjuntos: number;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/proveedores/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un proveedor */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del proveedor. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Proveedor del catálogo (global, enriquecido R15). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del proveedor. */
              id: number;
              /** @description Nombre del proveedor. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /**
               * @description Clasificación rápida: TELAS, AVIOS, SERVICIOS o SIN_CLASIFICAR.
               * @enum {string}
               */
              tipo: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Condiciones comerciales (texto libre), o null. */
              condiciones: string | null;
              /** @description ¿Emite CFDI? (formal/informal), o null. */
              factura: boolean | null;
              /** @description RFC, o null. */
              rfc: string | null;
              /** @description Régimen fiscal del SAT, o null. */
              regimenFiscalSat: string | null;
              /** @description Uso de CFDI habitual, o null. */
              usoCfdiHabitual: string | null;
              /** @description CP de expedición, o null. */
              codigoPostalExpedicion: string | null;
              /** @description ¿Se le retiene IVA?, o null. */
              retieneIva: boolean | null;
              /** @description ¿Se le retiene ISR?, o null. */
              retieneIsr: boolean | null;
              /** @description Email (para OC y XML), o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Días de crédito (null/0 = contado). */
              diasCredito: number | null;
              /** @description Moneda habitual (MXN/USD), o null. */
              moneda: string | null;
              /** @description Forma de pago, o null. */
              formaPago: string | null;
              /** @description Método de pago CFDI (PUE/PPD), o null. */
              metodoPago: string | null;
              /** @description Banco, o null. */
              banco: string | null;
              /** @description CLABE interbancaria, o null. */
              clabe: string | null;
              /** @description Límite de crédito, o null. */
              limiteCredito: number | null;
              /** @description Lead time en días, o null. */
              leadTimeDias: number | null;
              /** @description Notas, o null. */
              notas: string | null;
              /** @description Código corto del taller (ex maquilero), o null. */
              corto: string | null;
              /** @description ¿Está asegurado? (talleres), o null. */
              asegurado: boolean | null;
              /** @description Observaciones de pago (talleres), o null. */
              obsPago: string | null;
              /** @description Roles/servicios del proveedor. */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Clave estable del rol (kebab-case). */
                codigo: string;
                /** @description Nombre legible del rol. */
                nombre: string;
              }[];
              /** @description Cantidad de adjuntos del proveedor. */
              cantidadAdjuntos: number;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un proveedor (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del proveedor. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Proveedor del catálogo (global, enriquecido R15). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del proveedor. */
              id: number;
              /** @description Nombre del proveedor. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /**
               * @description Clasificación rápida: TELAS, AVIOS, SERVICIOS o SIN_CLASIFICAR.
               * @enum {string}
               */
              tipo: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Condiciones comerciales (texto libre), o null. */
              condiciones: string | null;
              /** @description ¿Emite CFDI? (formal/informal), o null. */
              factura: boolean | null;
              /** @description RFC, o null. */
              rfc: string | null;
              /** @description Régimen fiscal del SAT, o null. */
              regimenFiscalSat: string | null;
              /** @description Uso de CFDI habitual, o null. */
              usoCfdiHabitual: string | null;
              /** @description CP de expedición, o null. */
              codigoPostalExpedicion: string | null;
              /** @description ¿Se le retiene IVA?, o null. */
              retieneIva: boolean | null;
              /** @description ¿Se le retiene ISR?, o null. */
              retieneIsr: boolean | null;
              /** @description Email (para OC y XML), o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Días de crédito (null/0 = contado). */
              diasCredito: number | null;
              /** @description Moneda habitual (MXN/USD), o null. */
              moneda: string | null;
              /** @description Forma de pago, o null. */
              formaPago: string | null;
              /** @description Método de pago CFDI (PUE/PPD), o null. */
              metodoPago: string | null;
              /** @description Banco, o null. */
              banco: string | null;
              /** @description CLABE interbancaria, o null. */
              clabe: string | null;
              /** @description Límite de crédito, o null. */
              limiteCredito: number | null;
              /** @description Lead time en días, o null. */
              leadTimeDias: number | null;
              /** @description Notas, o null. */
              notas: string | null;
              /** @description Código corto del taller (ex maquilero), o null. */
              corto: string | null;
              /** @description ¿Está asegurado? (talleres), o null. */
              asegurado: boolean | null;
              /** @description Observaciones de pago (talleres), o null. */
              obsPago: string | null;
              /** @description Roles/servicios del proveedor. */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Clave estable del rol (kebab-case). */
                codigo: string;
                /** @description Nombre legible del rol. */
                nombre: string;
              }[];
              /** @description Cantidad de adjuntos del proveedor. */
              cantidadAdjuntos: number;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un proveedor */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del proveedor. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            razonSocial?: string | null;
            /** @enum {string} */
            tipo?: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
            telefono?: string | null;
            contacto?: string | null;
            condiciones?: string | null;
            roles?: number[];
            activo?: boolean;
            factura?: boolean;
            rfc?: string | null;
            regimenFiscalSat?: string | null;
            usoCfdiHabitual?: string | null;
            codigoPostalExpedicion?: string | null;
            retieneIva?: boolean;
            retieneIsr?: boolean;
            email?: string | null;
            direccion?: string | null;
            diasCredito?: number | null;
            moneda?: ('MXN' | 'USD') | null;
            formaPago?: string | null;
            metodoPago?: ('PUE' | 'PPD') | null;
            banco?: string | null;
            clabe?: string | null;
            limiteCredito?: number | null;
            leadTimeDias?: number | null;
            notas?: string | null;
            corto?: string | null;
            asegurado?: boolean;
            obsPago?: string | null;
          };
        };
      };
      responses: {
        /** @description Proveedor del catálogo (global, enriquecido R15). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del proveedor. */
              id: number;
              /** @description Nombre del proveedor. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /**
               * @description Clasificación rápida: TELAS, AVIOS, SERVICIOS o SIN_CLASIFICAR.
               * @enum {string}
               */
              tipo: 'TELAS' | 'AVIOS' | 'SERVICIOS' | 'SIN_CLASIFICAR';
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Condiciones comerciales (texto libre), o null. */
              condiciones: string | null;
              /** @description ¿Emite CFDI? (formal/informal), o null. */
              factura: boolean | null;
              /** @description RFC, o null. */
              rfc: string | null;
              /** @description Régimen fiscal del SAT, o null. */
              regimenFiscalSat: string | null;
              /** @description Uso de CFDI habitual, o null. */
              usoCfdiHabitual: string | null;
              /** @description CP de expedición, o null. */
              codigoPostalExpedicion: string | null;
              /** @description ¿Se le retiene IVA?, o null. */
              retieneIva: boolean | null;
              /** @description ¿Se le retiene ISR?, o null. */
              retieneIsr: boolean | null;
              /** @description Email (para OC y XML), o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Días de crédito (null/0 = contado). */
              diasCredito: number | null;
              /** @description Moneda habitual (MXN/USD), o null. */
              moneda: string | null;
              /** @description Forma de pago, o null. */
              formaPago: string | null;
              /** @description Método de pago CFDI (PUE/PPD), o null. */
              metodoPago: string | null;
              /** @description Banco, o null. */
              banco: string | null;
              /** @description CLABE interbancaria, o null. */
              clabe: string | null;
              /** @description Límite de crédito, o null. */
              limiteCredito: number | null;
              /** @description Lead time en días, o null. */
              leadTimeDias: number | null;
              /** @description Notas, o null. */
              notas: string | null;
              /** @description Código corto del taller (ex maquilero), o null. */
              corto: string | null;
              /** @description ¿Está asegurado? (talleres), o null. */
              asegurado: boolean | null;
              /** @description Observaciones de pago (talleres), o null. */
              obsPago: string | null;
              /** @description Roles/servicios del proveedor. */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Clave estable del rol (kebab-case). */
                codigo: string;
                /** @description Nombre legible del rol. */
                nombre: string;
              }[];
              /** @description Cantidad de adjuntos del proveedor. */
              cantidadAdjuntos: number;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/proveedores/{id}/adjuntos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar los adjuntos de un proveedor */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del proveedor. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Adjuntos de un proveedor. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Adjuntos del proveedor. */
              datos: {
                /** @description Id del registro Archivo. */
                idArchivo: string;
                /**
                 * @description Tipo documental del adjunto.
                 * @enum {string}
                 */
                tipo: 'CONSTANCIA' | 'CONTRATO' | 'OTRO';
                /** @description Nombre original del archivo. */
                nombreOriginal: string;
                /** @description Tipo MIME del archivo. */
                tipoMime: string;
                /** @description Tamaño en bytes. */
                tamanoBytes: number;
                /** @description URL GET prefirmada para ver/descargar el archivo. */
                urlDescarga: string;
                /**
                 * Format: date-time
                 * @description Fecha en que se adjuntó (ISO 8601).
                 */
                creadoEn: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Preparar la subida de un adjunto del proveedor */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del proveedor. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Datos para preparar la subida de un adjunto de proveedor. */
      requestBody: {
        content: {
          'application/json': {
            /**
             * @description Tipo documental del adjunto (constancia/contrato/otro).
             * @default OTRO
             * @enum {string}
             */
            tipo?: 'CONSTANCIA' | 'CONTRATO' | 'OTRO';
            /** @description Nombre del archivo tal como lo llama el usuario. */
            nombreOriginal: string;
            /** @description Tipo MIME del archivo (ej. application/pdf). */
            tipoMime: string;
            /** @description Tamaño exacto en bytes (la URL prefirmada solo acepta este tamaño). */
            tamanoBytes: number;
          };
        };
      };
      responses: {
        /** @description Resultado de preparar la subida de un adjunto (URL prefirmada). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del registro Archivo creado. */
              idArchivo: string;
              /**
               * @description Tipo documental del adjunto.
               * @enum {string}
               */
              tipo: 'CONSTANCIA' | 'CONTRATO' | 'OTRO';
              /** @description Nombre original del archivo. */
              nombreOriginal: string;
              /** @description URL PUT prefirmada: el navegador sube directo a R2. */
              urlSubida: string;
              /** @description Vigencia de la URL de subida (segundos). */
              expiraEnSegundos: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/proveedores/{id}/adjuntos/{idArchivo}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    post?: never;
    /** Quitar un adjunto del proveedor */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del proveedor. */
          id: number;
          /** @description Id del adjunto. */
          idArchivo: string;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        204: {
          headers: {
            [name: string]: unknown;
          };
          content?: never;
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/temporadas': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar temporadas */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Incluye las desactivadas ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de temporadas. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Temporadas de la página. */
              datos: {
                /** @description Id de la temporada. */
                id: number;
                /** @description Nombre de la temporada. */
                nombre: string;
                /** @description Falso si está desactivada (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que la creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que la modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de temporadas que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una temporada */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
          };
        };
      };
      responses: {
        /** @description Temporada del catálogo (global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la temporada. */
              id: number;
              /** @description Nombre de la temporada. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/temporadas/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener una temporada */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la temporada. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Temporada del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la temporada. */
              id: number;
              /** @description Nombre de la temporada. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar una temporada (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la temporada. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Temporada del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la temporada. */
              id: number;
              /** @description Nombre de la temporada. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar una temporada */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la temporada. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Temporada del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la temporada. */
              id: number;
              /** @description Nombre de la temporada. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/etiquetas-marca': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar etiquetas de marca */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Incluye las desactivadas ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'nombre' | 'regalias' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de etiquetas de marca. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Etiquetas de marca de la página. */
              datos: {
                /** @description Id de la etiqueta de marca. */
                id: number;
                /** @description Nombre de la etiqueta de marca. */
                nombre: string;
                /** @description Porcentaje de regalías (0–100). */
                regalias: number;
                /** @description Falso si está desactivada (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que la creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que la modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de etiquetas que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una etiqueta de marca */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            /** @default 0 */
            regalias?: number;
          };
        };
      };
      responses: {
        /** @description Etiqueta de marca del catálogo (global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etiqueta de marca. */
              id: number;
              /** @description Nombre de la etiqueta de marca. */
              nombre: string;
              /** @description Porcentaje de regalías (0–100). */
              regalias: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/etiquetas-marca/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener una etiqueta de marca */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la etiqueta de marca. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Etiqueta de marca del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etiqueta de marca. */
              id: number;
              /** @description Nombre de la etiqueta de marca. */
              nombre: string;
              /** @description Porcentaje de regalías (0–100). */
              regalias: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar una etiqueta de marca (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la etiqueta de marca. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Etiqueta de marca del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etiqueta de marca. */
              id: number;
              /** @description Nombre de la etiqueta de marca. */
              nombre: string;
              /** @description Porcentaje de regalías (0–100). */
              regalias: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar una etiqueta de marca */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la etiqueta de marca. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            regalias?: number;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Etiqueta de marca del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etiqueta de marca. */
              id: number;
              /** @description Nombre de la etiqueta de marca. */
              nombre: string;
              /** @description Porcentaje de regalías (0–100). */
              regalias: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/colores': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar colores */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de colores. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Colores de la página. */
              datos: {
                /** @description Id del color. */
                id: number;
                /** @description Nombre del color (normalizado). */
                nombre: string;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de colores que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un color */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
          };
        };
      };
      responses: {
        /** @description Color del catálogo (global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del color. */
              id: number;
              /** @description Nombre del color (normalizado). */
              nombre: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/colores/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un color */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del color. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Color del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del color. */
              id: number;
              /** @description Nombre del color (normalizado). */
              nombre: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un color (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del color. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Color del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del color. */
              id: number;
              /** @description Nombre del color (normalizado). */
              nombre: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un color */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del color. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Color del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del color. */
              id: number;
              /** @description Nombre del color (normalizado). */
              nombre: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/colores/fusionar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Fusionar colores duplicados en uno canónico */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      /** @description Fusión de colores duplicados: orígenes → destino canónico. */
      requestBody: {
        content: {
          'application/json': {
            /** @description Id del color CANÓNICO que se conserva (destino de la fusión). */
            idDestino: number;
            /** @description Ids de los colores DUPLICADOS que se absorben en el destino. */
            origenes: number[];
          };
        };
      };
      responses: {
        /** @description Color del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del color. */
              id: number;
              /** @description Nombre del color (normalizado). */
              nombre: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/tallas': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar tallas */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página. */
          porPagina?: number;
          /** @description Texto a buscar en la etiqueta. */
          busqueda?: string;
          /** @description Incluye las desactivadas. */
          incluirInactivos?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'etiqueta' | 'orden' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de tallas. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Tallas de la página. */
              datos: {
                /** @description Id de la talla. */
                id: number;
                /** @description Etiqueta de la talla (XCH, CH, M…). */
                etiqueta: string;
                /** @description Orden canónico de despliegue. */
                orden: number;
                /** @description Falso si está desactivada (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que la creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que la modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de tallas que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una talla */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            etiqueta: string;
            orden?: number;
          };
        };
      };
      responses: {
        /** @description Talla del catálogo (global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la talla. */
              id: number;
              /** @description Etiqueta de la talla (XCH, CH, M…). */
              etiqueta: string;
              /** @description Orden canónico de despliegue. */
              orden: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/tallas/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener una talla */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la talla. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Talla del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la talla. */
              id: number;
              /** @description Etiqueta de la talla (XCH, CH, M…). */
              etiqueta: string;
              /** @description Orden canónico de despliegue. */
              orden: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar una talla (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la talla. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Talla del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la talla. */
              id: number;
              /** @description Etiqueta de la talla (XCH, CH, M…). */
              etiqueta: string;
              /** @description Orden canónico de despliegue. */
              orden: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar una talla */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la talla. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            etiqueta?: string;
            orden?: number;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Talla del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la talla. */
              id: number;
              /** @description Etiqueta de la talla (XCH, CH, M…). */
              etiqueta: string;
              /** @description Orden canónico de despliegue. */
              orden: number;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/curvas-talla': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar curvas de tallas */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página. */
          porPagina?: number;
          /** @description Texto a buscar en el nombre. */
          busqueda?: string;
          /** @description Incluye las desactivadas. */
          incluirInactivos?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de curvas. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Curvas de la página. */
              datos: {
                /** @description Id de la curva. */
                id: number;
                /** @description Nombre de la curva. */
                nombre: string;
                /** @description Falso si está desactivada (borrado suave). */
                activo: boolean;
                /** @description Tallas de la curva, en orden. */
                items: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla. */
                  etiqueta: string;
                  /** @description Posición de la talla dentro de la curva (0-based). */
                  posicion: number;
                }[];
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que la creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que la modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de curvas que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una curva de tallas */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            items: number[];
          };
        };
      };
      responses: {
        /** @description Curva de tallas (global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la curva. */
              id: number;
              /** @description Nombre de la curva. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /** @description Tallas de la curva, en orden. */
              items: {
                /** @description Id de la talla. */
                idTalla: number;
                /** @description Etiqueta de la talla. */
                etiqueta: string;
                /** @description Posición de la talla dentro de la curva (0-based). */
                posicion: number;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/curvas-talla/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener una curva (con sus tallas en orden) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la curva. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Curva de tallas (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la curva. */
              id: number;
              /** @description Nombre de la curva. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /** @description Tallas de la curva, en orden. */
              items: {
                /** @description Id de la talla. */
                idTalla: number;
                /** @description Etiqueta de la talla. */
                etiqueta: string;
                /** @description Posición de la talla dentro de la curva (0-based). */
                posicion: number;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar una curva (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la curva. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Curva de tallas (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la curva. */
              id: number;
              /** @description Nombre de la curva. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /** @description Tallas de la curva, en orden. */
              items: {
                /** @description Id de la talla. */
                idTalla: number;
                /** @description Etiqueta de la talla. */
                etiqueta: string;
                /** @description Posición de la talla dentro de la curva (0-based). */
                posicion: number;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar una curva de tallas */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la curva. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            items?: number[];
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Curva de tallas (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la curva. */
              id: number;
              /** @description Nombre de la curva. */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /** @description Tallas de la curva, en orden. */
              items: {
                /** @description Id de la talla. */
                idTalla: number;
                /** @description Etiqueta de la talla. */
                etiqueta: string;
                /** @description Posición de la talla dentro de la curva (0-based). */
                posicion: number;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/clientes': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar clientes */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página. */
          porPagina?: number;
          /** @description Texto a buscar en el nombre. */
          busqueda?: string;
          /** @description Incluye los desactivados. */
          incluirInactivos?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de clientes. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Clientes de la página. */
              datos: {
                /** @description Id del cliente. */
                id: number;
                /** @description Nombre del cliente. */
                nombre: string;
                /** @description Persona de contacto, o null. */
                contacto: string | null;
                /** @description Teléfono, o null. */
                telefono: string | null;
                /** @description Email, o null. */
                email: string | null;
                /** @description Dirección, o null. */
                direccion: string | null;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
                /** @description Campos de referencia del cliente (D7), ordenados por `orden`. */
                campos: {
                  /** @description Id del campo. */
                  id: number;
                  /** @description Id del cliente dueño del campo. */
                  idCliente: number;
                  /** @description Etiqueta del campo (única por cliente). */
                  etiqueta: string;
                  /**
                   * @description Tipo de dato del valor (D7).
                   * @enum {string}
                   */
                  tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
                  /** @description Orden de despliegue dentro del cliente. */
                  orden: number;
                  /** @description Falso si está desactivado (borrado suave). */
                  activo: boolean;
                  /**
                   * Format: date-time
                   * @description Fecha de alta (ISO 8601).
                   */
                  creadoEn: string;
                  /** @description Id del usuario que lo creó. */
                  creadoPorId: string | null;
                  /**
                   * Format: date-time
                   * @description Fecha de la última modificación (ISO 8601).
                   */
                  modificadoEn: string;
                  /** @description Id del último usuario que lo modificó. */
                  modificadoPorId: string | null;
                }[];
              }[];
              /** @description Total de clientes que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un cliente */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            contacto?: string;
            telefono?: string;
            /** Format: email */
            email?: string;
            direccion?: string;
          };
        };
      };
      responses: {
        /** @description Cliente del catálogo (global), con sus campos de referencia (D7). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del cliente. */
              id: number;
              /** @description Nombre del cliente. */
              nombre: string;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Email, o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
              /** @description Campos de referencia del cliente (D7), ordenados por `orden`. */
              campos: {
                /** @description Id del campo. */
                id: number;
                /** @description Id del cliente dueño del campo. */
                idCliente: number;
                /** @description Etiqueta del campo (única por cliente). */
                etiqueta: string;
                /**
                 * @description Tipo de dato del valor (D7).
                 * @enum {string}
                 */
                tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
                /** @description Orden de despliegue dentro del cliente. */
                orden: number;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/clientes/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un cliente */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cliente. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Cliente del catálogo (global), con sus campos de referencia (D7). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del cliente. */
              id: number;
              /** @description Nombre del cliente. */
              nombre: string;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Email, o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
              /** @description Campos de referencia del cliente (D7), ordenados por `orden`. */
              campos: {
                /** @description Id del campo. */
                id: number;
                /** @description Id del cliente dueño del campo. */
                idCliente: number;
                /** @description Etiqueta del campo (única por cliente). */
                etiqueta: string;
                /**
                 * @description Tipo de dato del valor (D7).
                 * @enum {string}
                 */
                tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
                /** @description Orden de despliegue dentro del cliente. */
                orden: number;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un cliente (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cliente. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Cliente del catálogo (global), con sus campos de referencia (D7). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del cliente. */
              id: number;
              /** @description Nombre del cliente. */
              nombre: string;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Email, o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
              /** @description Campos de referencia del cliente (D7), ordenados por `orden`. */
              campos: {
                /** @description Id del campo. */
                id: number;
                /** @description Id del cliente dueño del campo. */
                idCliente: number;
                /** @description Etiqueta del campo (única por cliente). */
                etiqueta: string;
                /**
                 * @description Tipo de dato del valor (D7).
                 * @enum {string}
                 */
                tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
                /** @description Orden de despliegue dentro del cliente. */
                orden: number;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un cliente */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cliente. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            contacto?: string | null;
            telefono?: string | null;
            email?: string | null;
            direccion?: string | null;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Cliente del catálogo (global), con sus campos de referencia (D7). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del cliente. */
              id: number;
              /** @description Nombre del cliente. */
              nombre: string;
              /** @description Persona de contacto, o null. */
              contacto: string | null;
              /** @description Teléfono, o null. */
              telefono: string | null;
              /** @description Email, o null. */
              email: string | null;
              /** @description Dirección, o null. */
              direccion: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
              /** @description Campos de referencia del cliente (D7), ordenados por `orden`. */
              campos: {
                /** @description Id del campo. */
                id: number;
                /** @description Id del cliente dueño del campo. */
                idCliente: number;
                /** @description Etiqueta del campo (única por cliente). */
                etiqueta: string;
                /**
                 * @description Tipo de dato del valor (D7).
                 * @enum {string}
                 */
                tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
                /** @description Orden de despliegue dentro del cliente. */
                orden: number;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/clientes/{id}/campos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar los campos de referencia de un cliente (D7) */
    get: {
      parameters: {
        query?: {
          /** @description Incluye los campos desactivados ("true"/"false"). */
          incluirInactivos?: string;
        };
        header?: never;
        path: {
          /** @description Id del cliente. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Campos de referencia de un cliente (D7). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Campos de referencia del cliente. */
              datos: {
                /** @description Id del campo. */
                id: number;
                /** @description Id del cliente dueño del campo. */
                idCliente: number;
                /** @description Etiqueta del campo (única por cliente). */
                etiqueta: string;
                /**
                 * @description Tipo de dato del valor (D7).
                 * @enum {string}
                 */
                tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
                /** @description Orden de despliegue dentro del cliente. */
                orden: number;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Agregar un campo de referencia a un cliente (D7) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cliente. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            etiqueta: string;
            /**
             * @default TEXTO
             * @enum {string}
             */
            tipo?: 'TEXTO' | 'NUMERO' | 'FECHA';
            orden?: number;
          };
        };
      };
      responses: {
        /** @description Campo de referencia de un cliente (D7). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del campo. */
              id: number;
              /** @description Id del cliente dueño del campo. */
              idCliente: number;
              /** @description Etiqueta del campo (única por cliente). */
              etiqueta: string;
              /**
               * @description Tipo de dato del valor (D7).
               * @enum {string}
               */
              tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
              /** @description Orden de despliegue dentro del cliente. */
              orden: number;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/clientes/{id}/campos/{idCampo}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    post?: never;
    /** Desactivar un campo de referencia de un cliente (D7) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cliente. */
          id: number;
          /** @description Id del campo de referencia. */
          idCampo: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Campo de referencia de un cliente (D7). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del campo. */
              id: number;
              /** @description Id del cliente dueño del campo. */
              idCliente: number;
              /** @description Etiqueta del campo (única por cliente). */
              etiqueta: string;
              /**
               * @description Tipo de dato del valor (D7).
               * @enum {string}
               */
              tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
              /** @description Orden de despliegue dentro del cliente. */
              orden: number;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un campo de referencia de un cliente (D7) */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cliente. */
          id: number;
          /** @description Id del campo de referencia. */
          idCampo: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            etiqueta?: string;
            /** @enum {string} */
            tipo?: 'TEXTO' | 'NUMERO' | 'FECHA';
            orden?: number;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Campo de referencia de un cliente (D7). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del campo. */
              id: number;
              /** @description Id del cliente dueño del campo. */
              idCliente: number;
              /** @description Etiqueta del campo (única por cliente). */
              etiqueta: string;
              /**
               * @description Tipo de dato del valor (D7).
               * @enum {string}
               */
              tipo: 'TEXTO' | 'NUMERO' | 'FECHA';
              /** @description Orden de despliegue dentro del cliente. */
              orden: number;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/telas-categorias': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar categorías de tela */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Incluye las desactivadas ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de categorías de tela. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Categorías de la página. */
              datos: {
                /** @description Id de la categoría. */
                id: number;
                /** @description Nombre de la categoría (único global). */
                nombre: string;
                /** @description Falso si está desactivada (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que la creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que la modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de categorías que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una categoría de tela */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
          };
        };
      };
      responses: {
        /** @description Categoría de tela del catálogo (global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la categoría. */
              id: number;
              /** @description Nombre de la categoría (único global). */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/telas-categorias/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    post?: never;
    /** Desactivar una categoría de tela (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la categoría de tela. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Categoría de tela del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la categoría. */
              id: number;
              /** @description Nombre de la categoría (único global). */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar una categoría de tela */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la categoría de tela. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Categoría de tela del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la categoría. */
              id: number;
              /** @description Nombre de la categoría (único global). */
              nombre: string;
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/telas': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar telas */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Filtra por id de categoría. */
          idCategoria?: number;
          /** @description Incluye las desactivadas ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de telas. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Telas de la página. */
              datos: {
                /** @description Id de la tela. */
                id: number;
                /** @description Nombre de la tela (único global). */
                nombre: string;
                /** @description Descripción, o null. */
                descripcion: string | null;
                /** @description Id de la categoría, o null. */
                idCategoria: number | null;
                /** @description Nombre de la categoría, o null. */
                categoria: string | null;
                /** @description Unidad de medida (kg, m…), o null. */
                unidadMedida: string | null;
                /**
                 * @description Rol típico de la tela en el lote (D5).
                 * @enum {string}
                 */
                tipoComponente: 'CUERPO' | 'CARDIGAN' | 'OTRO';
                /** @description ¿Tela de uso frecuente? */
                favorito: boolean;
                /** @description Precio de referencia por unidad, o null. */
                precioSugerido: number | null;
                /** @description ¿Es tela de producción (vs. muestra/insumo)? */
                paraProduccion: boolean;
                /** @description Colores de la tela con su precio (N:N). */
                colores: {
                  /** @description Id del color. */
                  idColor: number;
                  /** @description Nombre del color. */
                  nombre: string;
                  /** @description Precio de la tela en este color, o null. */
                  precio: number | null;
                }[];
                /** @description Falso si está desactivada (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que la creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que la modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de telas que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una tela */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            idCategoria?: number;
            /**
             * @default OTRO
             * @enum {string}
             */
            tipoComponente?: 'CUERPO' | 'CARDIGAN' | 'OTRO';
            /** @default false */
            favorito?: boolean;
            /** @default true */
            paraProduccion?: boolean;
            precioSugerido?: number;
            /** @default [] */
            colores?: {
              idColor: number;
              precio?: number;
            }[];
            descripcion?: string;
            unidadMedida?: string;
          };
        };
      };
      responses: {
        /** @description Tela unificada del catálogo (global), con sus colores. */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la tela. */
              id: number;
              /** @description Nombre de la tela (único global). */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Id de la categoría, o null. */
              idCategoria: number | null;
              /** @description Nombre de la categoría, o null. */
              categoria: string | null;
              /** @description Unidad de medida (kg, m…), o null. */
              unidadMedida: string | null;
              /**
               * @description Rol típico de la tela en el lote (D5).
               * @enum {string}
               */
              tipoComponente: 'CUERPO' | 'CARDIGAN' | 'OTRO';
              /** @description ¿Tela de uso frecuente? */
              favorito: boolean;
              /** @description Precio de referencia por unidad, o null. */
              precioSugerido: number | null;
              /** @description ¿Es tela de producción (vs. muestra/insumo)? */
              paraProduccion: boolean;
              /** @description Colores de la tela con su precio (N:N). */
              colores: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                nombre: string;
                /** @description Precio de la tela en este color, o null. */
                precio: number | null;
              }[];
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/telas/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener una tela */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la tela. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Tela unificada del catálogo (global), con sus colores. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la tela. */
              id: number;
              /** @description Nombre de la tela (único global). */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Id de la categoría, o null. */
              idCategoria: number | null;
              /** @description Nombre de la categoría, o null. */
              categoria: string | null;
              /** @description Unidad de medida (kg, m…), o null. */
              unidadMedida: string | null;
              /**
               * @description Rol típico de la tela en el lote (D5).
               * @enum {string}
               */
              tipoComponente: 'CUERPO' | 'CARDIGAN' | 'OTRO';
              /** @description ¿Tela de uso frecuente? */
              favorito: boolean;
              /** @description Precio de referencia por unidad, o null. */
              precioSugerido: number | null;
              /** @description ¿Es tela de producción (vs. muestra/insumo)? */
              paraProduccion: boolean;
              /** @description Colores de la tela con su precio (N:N). */
              colores: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                nombre: string;
                /** @description Precio de la tela en este color, o null. */
                precio: number | null;
              }[];
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar una tela (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la tela. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Tela unificada del catálogo (global), con sus colores. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la tela. */
              id: number;
              /** @description Nombre de la tela (único global). */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Id de la categoría, o null. */
              idCategoria: number | null;
              /** @description Nombre de la categoría, o null. */
              categoria: string | null;
              /** @description Unidad de medida (kg, m…), o null. */
              unidadMedida: string | null;
              /**
               * @description Rol típico de la tela en el lote (D5).
               * @enum {string}
               */
              tipoComponente: 'CUERPO' | 'CARDIGAN' | 'OTRO';
              /** @description ¿Tela de uso frecuente? */
              favorito: boolean;
              /** @description Precio de referencia por unidad, o null. */
              precioSugerido: number | null;
              /** @description ¿Es tela de producción (vs. muestra/insumo)? */
              paraProduccion: boolean;
              /** @description Colores de la tela con su precio (N:N). */
              colores: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                nombre: string;
                /** @description Precio de la tela en este color, o null. */
                precio: number | null;
              }[];
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar una tela */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la tela. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            idCategoria?: number | null;
            /** @enum {string} */
            tipoComponente?: 'CUERPO' | 'CARDIGAN' | 'OTRO';
            favorito?: boolean;
            paraProduccion?: boolean;
            precioSugerido?: number | null;
            colores?: {
              idColor: number;
              precio?: number;
            }[];
            activo?: boolean;
            descripcion?: string | null;
            unidadMedida?: string | null;
          };
        };
      };
      responses: {
        /** @description Tela unificada del catálogo (global), con sus colores. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la tela. */
              id: number;
              /** @description Nombre de la tela (único global). */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Id de la categoría, o null. */
              idCategoria: number | null;
              /** @description Nombre de la categoría, o null. */
              categoria: string | null;
              /** @description Unidad de medida (kg, m…), o null. */
              unidadMedida: string | null;
              /**
               * @description Rol típico de la tela en el lote (D5).
               * @enum {string}
               */
              tipoComponente: 'CUERPO' | 'CARDIGAN' | 'OTRO';
              /** @description ¿Tela de uso frecuente? */
              favorito: boolean;
              /** @description Precio de referencia por unidad, o null. */
              precioSugerido: number | null;
              /** @description ¿Es tela de producción (vs. muestra/insumo)? */
              paraProduccion: boolean;
              /** @description Colores de la tela con su precio (N:N). */
              colores: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                nombre: string;
                /** @description Precio de la tela en este color, o null. */
                precio: number | null;
              }[];
              /** @description Falso si está desactivada (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/telas/{id}/colores': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar los colores de una tela (con su precio) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la tela. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Colores de una tela (N:N a Color). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Colores de la tela con su precio. */
              datos: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                nombre: string;
                /** @description Precio de la tela en este color, o null. */
                precio: number | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/avios': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar avíos */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en la clave o la descripción (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Filtra por avíos genéricos (R4): "true"/"false". Omitir = todos. */
          esGenerico?: string;
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'clave' | 'descripcion' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de avíos. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Avíos de la página. */
              datos: {
                /** @description Id del avío. */
                id: number;
                /** @description Clave de negocio del avío (única global). */
                clave: string;
                /** @description Descripción del avío. */
                descripcion: string;
                /** @description Unidad de medida (pza, m, kg…), o null. */
                unidad: string | null;
                /** @description Presentación/empaque, o null. */
                presentacion: string | null;
                /** @description ¿Avío de uso frecuente? */
                favorito: boolean;
                /** @description Cantidad preestablecida si es favorito, o null. */
                cantFav: number | null;
                /** @description ¿Avío genérico de stock (R4)? */
                esGenerico: boolean;
                /** @description Precio de referencia (fallback), o null. */
                precioReferencia: number | null;
                /** @description Proveedores del avío con su precio y condiciones (R1). */
                proveedores: {
                  /** @description Id del proveedor. */
                  idProveedor: number;
                  /** @description Nombre del proveedor (para la UI). */
                  nombreProveedor: string;
                  /** @description Precio al que este proveedor surte el avío, o null. */
                  precio: number | null;
                  /** @description Condiciones de este proveedor, o null. */
                  condiciones: string | null;
                }[];
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de avíos que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un avío */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            clave: string;
            descripcion: string;
            unidad?: string;
            presentacion?: string;
            /** @default false */
            favorito?: boolean;
            cantFav?: number;
            /** @default false */
            esGenerico?: boolean;
            precioReferencia?: number;
            proveedores?: {
              idProveedor: number;
              precio?: number;
              condiciones?: string;
            }[];
          };
        };
      };
      responses: {
        /** @description Avío del catálogo (global, R1). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del avío. */
              id: number;
              /** @description Clave de negocio del avío (única global). */
              clave: string;
              /** @description Descripción del avío. */
              descripcion: string;
              /** @description Unidad de medida (pza, m, kg…), o null. */
              unidad: string | null;
              /** @description Presentación/empaque, o null. */
              presentacion: string | null;
              /** @description ¿Avío de uso frecuente? */
              favorito: boolean;
              /** @description Cantidad preestablecida si es favorito, o null. */
              cantFav: number | null;
              /** @description ¿Avío genérico de stock (R4)? */
              esGenerico: boolean;
              /** @description Precio de referencia (fallback), o null. */
              precioReferencia: number | null;
              /** @description Proveedores del avío con su precio y condiciones (R1). */
              proveedores: {
                /** @description Id del proveedor. */
                idProveedor: number;
                /** @description Nombre del proveedor (para la UI). */
                nombreProveedor: string;
                /** @description Precio al que este proveedor surte el avío, o null. */
                precio: number | null;
                /** @description Condiciones de este proveedor, o null. */
                condiciones: string | null;
              }[];
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/avios/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un avío */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del avío. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Avío del catálogo (global, R1). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del avío. */
              id: number;
              /** @description Clave de negocio del avío (única global). */
              clave: string;
              /** @description Descripción del avío. */
              descripcion: string;
              /** @description Unidad de medida (pza, m, kg…), o null. */
              unidad: string | null;
              /** @description Presentación/empaque, o null. */
              presentacion: string | null;
              /** @description ¿Avío de uso frecuente? */
              favorito: boolean;
              /** @description Cantidad preestablecida si es favorito, o null. */
              cantFav: number | null;
              /** @description ¿Avío genérico de stock (R4)? */
              esGenerico: boolean;
              /** @description Precio de referencia (fallback), o null. */
              precioReferencia: number | null;
              /** @description Proveedores del avío con su precio y condiciones (R1). */
              proveedores: {
                /** @description Id del proveedor. */
                idProveedor: number;
                /** @description Nombre del proveedor (para la UI). */
                nombreProveedor: string;
                /** @description Precio al que este proveedor surte el avío, o null. */
                precio: number | null;
                /** @description Condiciones de este proveedor, o null. */
                condiciones: string | null;
              }[];
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un avío (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del avío. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Avío del catálogo (global, R1). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del avío. */
              id: number;
              /** @description Clave de negocio del avío (única global). */
              clave: string;
              /** @description Descripción del avío. */
              descripcion: string;
              /** @description Unidad de medida (pza, m, kg…), o null. */
              unidad: string | null;
              /** @description Presentación/empaque, o null. */
              presentacion: string | null;
              /** @description ¿Avío de uso frecuente? */
              favorito: boolean;
              /** @description Cantidad preestablecida si es favorito, o null. */
              cantFav: number | null;
              /** @description ¿Avío genérico de stock (R4)? */
              esGenerico: boolean;
              /** @description Precio de referencia (fallback), o null. */
              precioReferencia: number | null;
              /** @description Proveedores del avío con su precio y condiciones (R1). */
              proveedores: {
                /** @description Id del proveedor. */
                idProveedor: number;
                /** @description Nombre del proveedor (para la UI). */
                nombreProveedor: string;
                /** @description Precio al que este proveedor surte el avío, o null. */
                precio: number | null;
                /** @description Condiciones de este proveedor, o null. */
                condiciones: string | null;
              }[];
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un avío */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del avío. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            clave?: string;
            descripcion?: string;
            unidad?: string | null;
            presentacion?: string | null;
            favorito?: boolean;
            cantFav?: number | null;
            esGenerico?: boolean;
            precioReferencia?: number | null;
            proveedores?: {
              idProveedor: number;
              precio?: number;
              condiciones?: string;
            }[];
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Avío del catálogo (global, R1). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del avío. */
              id: number;
              /** @description Clave de negocio del avío (única global). */
              clave: string;
              /** @description Descripción del avío. */
              descripcion: string;
              /** @description Unidad de medida (pza, m, kg…), o null. */
              unidad: string | null;
              /** @description Presentación/empaque, o null. */
              presentacion: string | null;
              /** @description ¿Avío de uso frecuente? */
              favorito: boolean;
              /** @description Cantidad preestablecida si es favorito, o null. */
              cantFav: number | null;
              /** @description ¿Avío genérico de stock (R4)? */
              esGenerico: boolean;
              /** @description Precio de referencia (fallback), o null. */
              precioReferencia: number | null;
              /** @description Proveedores del avío con su precio y condiciones (R1). */
              proveedores: {
                /** @description Id del proveedor. */
                idProveedor: number;
                /** @description Nombre del proveedor (para la UI). */
                nombreProveedor: string;
                /** @description Precio al que este proveedor surte el avío, o null. */
                precio: number | null;
                /** @description Condiciones de este proveedor, o null. */
                condiciones: string | null;
              }[];
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/avios/{id}/proveedores': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar los proveedores de un avío (con su precio y condiciones) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del avío. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Proveedores de un avío (R1). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Proveedores del avío con su precio. */
              datos: {
                /** @description Id del proveedor. */
                idProveedor: number;
                /** @description Nombre del proveedor (para la UI). */
                nombreProveedor: string;
                /** @description Precio al que este proveedor surte el avío, o null. */
                precio: number | null;
                /** @description Condiciones de este proveedor, o null. */
                condiciones: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/bordados': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar bordados/estampados */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Filtra por tipo (BORDADO/ESTAMPADO). */
          tipo?: 'BORDADO' | 'ESTAMPADO';
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'nombre' | 'tipo' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de bordados. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Bordados de la página. */
              datos: {
                /** @description Id del bordado. */
                id: number;
                /** @description Nombre del bordado/estampado. */
                nombre: string;
                /** @description Descripción, o null. */
                descripcion: string | null;
                /** @description Número de puntadas (informativo), o null. */
                puntadas: number | null;
                /** @description Precio de referencia, o null. */
                precio: number | null;
                /**
                 * @description BORDADO real o ESTAMPADO/aplicación.
                 * @enum {string}
                 */
                tipo: 'BORDADO' | 'ESTAMPADO';
                /** @description Id del Archivo de la foto en R2, o null si no tiene foto. */
                idArchivoFoto: string | null;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de bordados que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un bordado/estampado */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            descripcion?: string;
            puntadas?: number;
            precio?: number;
            /**
             * @default BORDADO
             * @enum {string}
             */
            tipo?: 'BORDADO' | 'ESTAMPADO';
          };
        };
      };
      responses: {
        /** @description Bordado/estampado del catálogo (global, con foto opcional en R2). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del bordado. */
              id: number;
              /** @description Nombre del bordado/estampado. */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Número de puntadas (informativo), o null. */
              puntadas: number | null;
              /** @description Precio de referencia, o null. */
              precio: number | null;
              /**
               * @description BORDADO real o ESTAMPADO/aplicación.
               * @enum {string}
               */
              tipo: 'BORDADO' | 'ESTAMPADO';
              /** @description Id del Archivo de la foto en R2, o null si no tiene foto. */
              idArchivoFoto: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/bordados/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un bordado */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del bordado. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Bordado/estampado del catálogo (global, con foto opcional en R2). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del bordado. */
              id: number;
              /** @description Nombre del bordado/estampado. */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Número de puntadas (informativo), o null. */
              puntadas: number | null;
              /** @description Precio de referencia, o null. */
              precio: number | null;
              /**
               * @description BORDADO real o ESTAMPADO/aplicación.
               * @enum {string}
               */
              tipo: 'BORDADO' | 'ESTAMPADO';
              /** @description Id del Archivo de la foto en R2, o null si no tiene foto. */
              idArchivoFoto: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un bordado (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del bordado. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Bordado/estampado del catálogo (global, con foto opcional en R2). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del bordado. */
              id: number;
              /** @description Nombre del bordado/estampado. */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Número de puntadas (informativo), o null. */
              puntadas: number | null;
              /** @description Precio de referencia, o null. */
              precio: number | null;
              /**
               * @description BORDADO real o ESTAMPADO/aplicación.
               * @enum {string}
               */
              tipo: 'BORDADO' | 'ESTAMPADO';
              /** @description Id del Archivo de la foto en R2, o null si no tiene foto. */
              idArchivoFoto: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un bordado */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del bordado. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            descripcion?: string | null;
            puntadas?: number | null;
            precio?: number | null;
            /** @enum {string} */
            tipo?: 'BORDADO' | 'ESTAMPADO';
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Bordado/estampado del catálogo (global, con foto opcional en R2). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del bordado. */
              id: number;
              /** @description Nombre del bordado/estampado. */
              nombre: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Número de puntadas (informativo), o null. */
              puntadas: number | null;
              /** @description Precio de referencia, o null. */
              precio: number | null;
              /**
               * @description BORDADO real o ESTAMPADO/aplicación.
               * @enum {string}
               */
              tipo: 'BORDADO' | 'ESTAMPADO';
              /** @description Id del Archivo de la foto en R2, o null si no tiene foto. */
              idArchivoFoto: string | null;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/bordados/{id}/foto': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener la URL de la foto de un bordado */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del bordado. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Foto de un bordado (con su URL de descarga) o vacía si no tiene. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del registro Archivo de la foto, o null. */
              idArchivo: string | null;
              /** @description Nombre original del archivo, o null. */
              nombreOriginal: string | null;
              /** @description Tipo MIME de la imagen, o null. */
              tipoMime: string | null;
              /** @description Tamaño en bytes, o null. */
              tamanoBytes: number | null;
              /** @description URL GET prefirmada para ver la foto, o null si no tiene foto. */
              urlDescarga: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Preparar la subida de la foto de un bordado */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del bordado. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Datos para preparar la subida de la foto de un bordado. */
      requestBody: {
        content: {
          'application/json': {
            /** @description Nombre del archivo tal como lo llama el usuario. */
            nombreOriginal: string;
            /** @description Tipo MIME de la imagen (ej. image/jpeg, image/png, image/webp). */
            tipoMime: string;
            /** @description Tamaño exacto en bytes (la URL prefirmada solo acepta este tamaño). */
            tamanoBytes: number;
          };
        };
      };
      responses: {
        /** @description Resultado de preparar la subida de la foto (URL prefirmada). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del registro Archivo creado para la foto. */
              idArchivo: string;
              /** @description Nombre original del archivo. */
              nombreOriginal: string;
              /** @description URL PUT prefirmada: el navegador sube directo a R2. */
              urlSubida: string;
              /** @description Vigencia de la URL de subida (segundos). */
              expiraEnSegundos: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    /** Quitar la foto de un bordado */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del bordado. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        204: {
          headers: {
            [name: string]: unknown;
          };
          content?: never;
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/generos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar géneros (catálogo selector) */
    get: {
      parameters: {
        query?: {
          /** @description Incluye los géneros desactivados ("true"/"false"). */
          incluirInactivos?: string;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del género. */
              id: number;
              /** @description Nombre del género (único global). */
              nombre: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
            }[];
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/modelos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar modelos */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el código o la descripción (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Filtra por id de temporada. */
          idTemporada?: number;
          /** @description Incluye los descontinuados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'codigo' | 'descripcion' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de modelos. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Modelos de la página. */
              datos: {
                /** @description Id del modelo. */
                id: number;
                /** @description Código/clave de negocio del modelo (único global). */
                codigo: string;
                /** @description Descripción, o null. */
                descripcion: string | null;
                /** @description Costo de maquila base, o null. */
                maquilaBase: number | null;
                /** @description Id de la temporada, o null. */
                idTemporada: number | null;
                /** @description Nombre de la temporada, o null. */
                temporada: string | null;
                /** @description Id de la curva de tallas, o null. */
                idCurvaTalla: number | null;
                /** @description Nombre de la curva de tallas, o null. */
                curvaTalla: string | null;
                /** @description Id del género, o null. */
                idGenero: number | null;
                /** @description Nombre del género, o null. */
                genero: string | null;
                /** @description Cantidad de fotos del modelo. */
                cantidadFotos: number;
                /** @description URL prefirmada de la foto principal del modelo, o null si no tiene fotos. */
                urlFotoPrincipal: string | null;
                /** @description Falso si está descontinuado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de modelos que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un modelo */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            codigo: string;
            maquilaBase?: number;
            idTemporada?: number;
            idCurvaTalla?: number;
            idGenero?: number;
            descripcion?: string;
          };
        };
      };
      responses: {
        /** @description Modelo del catálogo (global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del modelo. */
              id: number;
              /** @description Código/clave de negocio del modelo (único global). */
              codigo: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Costo de maquila base, o null. */
              maquilaBase: number | null;
              /** @description Id de la temporada, o null. */
              idTemporada: number | null;
              /** @description Nombre de la temporada, o null. */
              temporada: string | null;
              /** @description Id de la curva de tallas, o null. */
              idCurvaTalla: number | null;
              /** @description Nombre de la curva de tallas, o null. */
              curvaTalla: string | null;
              /** @description Id del género, o null. */
              idGenero: number | null;
              /** @description Nombre del género, o null. */
              genero: string | null;
              /** @description Cantidad de fotos del modelo. */
              cantidadFotos: number;
              /** @description URL prefirmada de la foto principal del modelo, o null si no tiene fotos. */
              urlFotoPrincipal: string | null;
              /** @description Falso si está descontinuado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/modelos/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener la ficha de un modelo (con su BOM) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Ficha de un modelo con su receta (BOM) completa. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del modelo. */
              id: number;
              /** @description Código/clave de negocio del modelo (único global). */
              codigo: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Costo de maquila base, o null. */
              maquilaBase: number | null;
              /** @description Id de la temporada, o null. */
              idTemporada: number | null;
              /** @description Nombre de la temporada, o null. */
              temporada: string | null;
              /** @description Id de la curva de tallas, o null. */
              idCurvaTalla: number | null;
              /** @description Nombre de la curva de tallas, o null. */
              curvaTalla: string | null;
              /** @description Id del género, o null. */
              idGenero: number | null;
              /** @description Nombre del género, o null. */
              genero: string | null;
              /** @description Cantidad de fotos del modelo. */
              cantidadFotos: number;
              /** @description URL prefirmada de la foto principal del modelo, o null si no tiene fotos. */
              urlFotoPrincipal: string | null;
              /** @description Falso si está descontinuado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
              /** @description Telas del BOM. */
              telas: {
                /** @description Id de la tela. */
                idTela: number;
                /** @description Nombre de la tela (para la UI). */
                nombre: string;
                /** @description Consumo de tela por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
              /** @description Avíos del BOM. */
              avios: {
                /** @description Id del avío. */
                idAvio: number;
                /** @description Clave del avío (para la UI). */
                clave: string;
                /** @description Descripción del avío (para la UI). */
                descripcion: string;
                /** @description Consumo de avío por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
              /** @description Bordados del BOM. */
              bordados: {
                /** @description Id del bordado. */
                idBordado: number;
                /** @description Nombre del bordado (para la UI). */
                nombre: string;
                /**
                 * @description Tipo del bordado (para la UI).
                 * @enum {string}
                 */
                tipo: 'BORDADO' | 'ESTAMPADO';
                /** @description Precio del bordado en este modelo, o null. */
                precio: number | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Descontinuar un modelo (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Modelo del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del modelo. */
              id: number;
              /** @description Código/clave de negocio del modelo (único global). */
              codigo: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Costo de maquila base, o null. */
              maquilaBase: number | null;
              /** @description Id de la temporada, o null. */
              idTemporada: number | null;
              /** @description Nombre de la temporada, o null. */
              temporada: string | null;
              /** @description Id de la curva de tallas, o null. */
              idCurvaTalla: number | null;
              /** @description Nombre de la curva de tallas, o null. */
              curvaTalla: string | null;
              /** @description Id del género, o null. */
              idGenero: number | null;
              /** @description Nombre del género, o null. */
              genero: string | null;
              /** @description Cantidad de fotos del modelo. */
              cantidadFotos: number;
              /** @description URL prefirmada de la foto principal del modelo, o null si no tiene fotos. */
              urlFotoPrincipal: string | null;
              /** @description Falso si está descontinuado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un modelo */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            codigo?: string;
            maquilaBase?: number | null;
            idTemporada?: number | null;
            idCurvaTalla?: number | null;
            idGenero?: number | null;
            descripcion?: string | null;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Modelo del catálogo (global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del modelo. */
              id: number;
              /** @description Código/clave de negocio del modelo (único global). */
              codigo: string;
              /** @description Descripción, o null. */
              descripcion: string | null;
              /** @description Costo de maquila base, o null. */
              maquilaBase: number | null;
              /** @description Id de la temporada, o null. */
              idTemporada: number | null;
              /** @description Nombre de la temporada, o null. */
              temporada: string | null;
              /** @description Id de la curva de tallas, o null. */
              idCurvaTalla: number | null;
              /** @description Nombre de la curva de tallas, o null. */
              curvaTalla: string | null;
              /** @description Id del género, o null. */
              idGenero: number | null;
              /** @description Nombre del género, o null. */
              genero: string | null;
              /** @description Cantidad de fotos del modelo. */
              cantidadFotos: number;
              /** @description URL prefirmada de la foto principal del modelo, o null si no tiene fotos. */
              urlFotoPrincipal: string | null;
              /** @description Falso si está descontinuado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/modelos/{id}/bom/telas': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar las telas del BOM de un modelo */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Telas del BOM de un modelo. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Telas del BOM. */
              datos: {
                /** @description Id de la tela. */
                idTela: number;
                /** @description Nombre de la tela (para la UI). */
                nombre: string;
                /** @description Consumo de tela por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    /** Reemplazar el set completo de telas del BOM */
    put: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Set completo de telas del BOM del modelo. */
      requestBody: {
        content: {
          'application/json': {
            telas: {
              idTela: number;
              consumoPorPrenda: number;
              /** @default true */
              paraPreCosto?: boolean;
              /** @default true */
              paraProduccion?: boolean;
              /** @default true */
              paraCosto?: boolean;
            }[];
          };
        };
      };
      responses: {
        /** @description Telas del BOM de un modelo. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Telas del BOM. */
              datos: {
                /** @description Id de la tela. */
                idTela: number;
                /** @description Nombre de la tela (para la UI). */
                nombre: string;
                /** @description Consumo de tela por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/modelos/{id}/bom/avios': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar los avíos del BOM de un modelo */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Avíos del BOM de un modelo. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Avíos del BOM. */
              datos: {
                /** @description Id del avío. */
                idAvio: number;
                /** @description Clave del avío (para la UI). */
                clave: string;
                /** @description Descripción del avío (para la UI). */
                descripcion: string;
                /** @description Consumo de avío por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    /** Reemplazar el set completo de avíos del BOM */
    put: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Set completo de avíos del BOM del modelo. */
      requestBody: {
        content: {
          'application/json': {
            avios: {
              idAvio: number;
              consumoPorPrenda: number;
              /** @default true */
              paraPreCosto?: boolean;
              /** @default true */
              paraProduccion?: boolean;
              /** @default true */
              paraCosto?: boolean;
            }[];
          };
        };
      };
      responses: {
        /** @description Avíos del BOM de un modelo. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Avíos del BOM. */
              datos: {
                /** @description Id del avío. */
                idAvio: number;
                /** @description Clave del avío (para la UI). */
                clave: string;
                /** @description Descripción del avío (para la UI). */
                descripcion: string;
                /** @description Consumo de avío por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/modelos/{id}/bom/bordados': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar los bordados del BOM de un modelo */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Bordados del BOM de un modelo. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Bordados del BOM. */
              datos: {
                /** @description Id del bordado. */
                idBordado: number;
                /** @description Nombre del bordado (para la UI). */
                nombre: string;
                /**
                 * @description Tipo del bordado (para la UI).
                 * @enum {string}
                 */
                tipo: 'BORDADO' | 'ESTAMPADO';
                /** @description Precio del bordado en este modelo, o null. */
                precio: number | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    /** Reemplazar el set completo de bordados del BOM */
    put: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Set completo de bordados del BOM del modelo. */
      requestBody: {
        content: {
          'application/json': {
            bordados: {
              idBordado: number;
              precio?: number;
            }[];
          };
        };
      };
      responses: {
        /** @description Bordados del BOM de un modelo. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Bordados del BOM. */
              datos: {
                /** @description Id del bordado. */
                idBordado: number;
                /** @description Nombre del bordado (para la UI). */
                nombre: string;
                /**
                 * @description Tipo del bordado (para la UI).
                 * @enum {string}
                 */
                tipo: 'BORDADO' | 'ESTAMPADO';
                /** @description Precio del bordado en este modelo, o null. */
                precio: number | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/modelos/{id}/copiar-bom': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Copiar la receta (BOM) de otro modelo */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Copiar la receta (BOM) de otro modelo. */
      requestBody: {
        content: {
          'application/json': {
            idOrigen: number;
            /** @default true */
            reemplazar?: boolean;
          };
        };
      };
      responses: {
        /** @description Default Response */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Telas del BOM. */
              telas: {
                /** @description Id de la tela. */
                idTela: number;
                /** @description Nombre de la tela (para la UI). */
                nombre: string;
                /** @description Consumo de tela por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
              /** @description Avíos del BOM. */
              avios: {
                /** @description Id del avío. */
                idAvio: number;
                /** @description Clave del avío (para la UI). */
                clave: string;
                /** @description Descripción del avío (para la UI). */
                descripcion: string;
                /** @description Consumo de avío por prenda. */
                consumoPorPrenda: number;
                /** @description ¿Entra en el pre-costeo? */
                paraPreCosto: boolean;
                /** @description ¿Se considera al producir? */
                paraProduccion: boolean;
                /** @description ¿Entra en el costeo real? */
                paraCosto: boolean;
              }[];
              /** @description Bordados del BOM. */
              bordados: {
                /** @description Id del bordado. */
                idBordado: number;
                /** @description Nombre del bordado (para la UI). */
                nombre: string;
                /**
                 * @description Tipo del bordado (para la UI).
                 * @enum {string}
                 */
                tipo: 'BORDADO' | 'ESTAMPADO';
                /** @description Precio del bordado en este modelo, o null. */
                precio: number | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/modelos/{id}/fotos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar las fotos de un modelo */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Fotos de un modelo (N por modelo). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Fotos del modelo (ordenadas). */
              datos: {
                /** @description Id del registro ModeloFoto. */
                idFoto: number;
                /** @description Id del registro Archivo de la foto. */
                idArchivo: string;
                /**
                 * @description Tipo de foto (frente/espalda/otra).
                 * @enum {string}
                 */
                tipo: 'FRENTE' | 'ESPALDA' | 'OTRO';
                /** @description Orden de despliegue en el carrusel. */
                orden: number;
                /** @description Nombre original del archivo. */
                nombreOriginal: string;
                /** @description Tipo MIME de la imagen. */
                tipoMime: string;
                /** @description Tamaño en bytes. */
                tamanoBytes: number;
                /** @description URL GET prefirmada para ver la foto. */
                urlDescarga: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Preparar la subida de una foto del modelo */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Datos para preparar la subida de una foto de un modelo. */
      requestBody: {
        content: {
          'application/json': {
            /** @description Nombre del archivo tal como lo llama el usuario. */
            nombreOriginal: string;
            /** @description Tipo MIME de la imagen (ej. image/jpeg, image/png, image/webp). */
            tipoMime: string;
            /** @description Tamaño exacto en bytes (la URL prefirmada solo acepta este tamaño). */
            tamanoBytes: number;
            /**
             * @description Tipo de foto (frente/espalda/otra).
             * @default OTRO
             * @enum {string}
             */
            tipo?: 'FRENTE' | 'ESPALDA' | 'OTRO';
          };
        };
      };
      responses: {
        /** @description Resultado de preparar la subida de una foto (URL prefirmada). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del registro ModeloFoto creado. */
              idFoto: number;
              /** @description Id del registro Archivo creado para la foto. */
              idArchivo: string;
              /** @description Nombre original del archivo. */
              nombreOriginal: string;
              /** @description URL PUT prefirmada: el navegador sube directo a R2. */
              urlSubida: string;
              /** @description Vigencia de la URL de subida (segundos). */
              expiraEnSegundos: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/modelos/{id}/fotos/{idFoto}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    post?: never;
    /** Quitar una foto del modelo */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
          /** @description Id de la foto. */
          idFoto: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        204: {
          headers: {
            [name: string]: unknown;
          };
          content?: never;
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar los metadatos de una foto (tipo/orden) */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del modelo. */
          id: number;
          /** @description Id de la foto. */
          idFoto: number;
        };
        cookie?: never;
      };
      /** @description Metadatos a actualizar de una foto del modelo (tipo/orden). */
      requestBody: {
        content: {
          'application/json': {
            /**
             * @description Nuevo tipo (frente/espalda/otra).
             * @enum {string}
             */
            tipo?: 'FRENTE' | 'ESPALDA' | 'OTRO';
            /** @description Nuevo orden de despliegue. */
            orden?: number;
          };
        };
      };
      responses: {
        /** @description Default Response */
        204: {
          headers: {
            [name: string]: unknown;
          };
          content?: never;
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/pedidos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar pedidos internos */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página. */
          porPagina?: number;
          /** @description Texto a buscar (folio o nombre del cliente). */
          busqueda?: string;
          /** @description Filtra por cliente. */
          idCliente?: number;
          /** @description Incluye los pedidos cancelados (cancelación suave). */
          incluirCancelados?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'folio' | 'fechaPedido' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de pedidos. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Pedidos de la página. */
              datos: {
                /** @description Id interno del pedido. */
                id: number;
                /** @description Folio consecutivo por empresa. */
                folio: number;
                /** @description Empresa dueña del pedido y del folio. */
                idEmpresa: number;
                /** @description Cliente del pedido. */
                idCliente: number;
                /** @description Nombre del cliente (para la UI). */
                cliente: string;
                /** @description Fecha del pedido (YYYY-MM-DD), o null. */
                fechaPedido: string | null;
                /** @description Ventana de entrega — desde, o null. */
                fechaDe: string | null;
                /** @description Ventana de entrega — hasta, o null. */
                fechaHasta: string | null;
                /** @description Fecha de tela, o null. */
                fechaTela: string | null;
                /** @description Fecha de elaboración, o null. */
                fechaElaboracion: string | null;
                /** @description Marca de entregado a tienda. */
                entregadoTienda: boolean;
                /** @description Marcado para no producir. */
                noProducir: boolean;
                /** @description Cancelación suave: el pedido sigue consultable. */
                pedCancelado: boolean;
                /** @description Snapshot migrado de SOLO LECTURA: orden de compra ligada en el viejo (sin FK hasta F4). */
                idOrdCompraV1: number | null;
                /** @description Suma de las cantidades pedidas de los renglones. */
                totalPiezas: number;
                /** @description Suma de los importes (Σ cantidad × precio), o null si no puede ver importes. */
                totalImporte: number | null;
                /** @description Renglones del pedido. */
                lineas: {
                  /** @description Id del renglón. */
                  id: number;
                  /** @description Id del modelo pedido. */
                  idModelo: number;
                  /** @description Código del modelo (para la UI). */
                  codigoModelo: string;
                  /** @description Descripción del modelo, o null. */
                  descripcionModelo: string | null;
                  /** @description URL prefirmada de la foto principal del modelo, o null. */
                  urlFotoModelo: string | null;
                  /** @description Cantidad pedida. */
                  cantidadPedida: number;
                  /** @description Precio por prenda, o null si la sesión no puede ver importes. */
                  precio: number | null;
                  /** @description Importe del renglón (cantidad × precio), o null si no puede ver importes. */
                  importe: number | null;
                  /** @description Snapshot migrado de SOLO LECTURA: cantidad ya entregada en el viejo (no saldo vivo). */
                  entregadoParcialV1: number | null;
                  /** @description Snapshot migrado de SOLO LECTURA: cantidad faltante en el viejo (no saldo vivo). */
                  cantFaltanteV1: number | null;
                }[];
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de pedidos que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un pedido interno */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Cliente del pedido. */
            idCliente: number;
            /** Format: date */
            fechaPedido?: string;
            /** Format: date */
            fechaDe?: string;
            /** Format: date */
            fechaHasta?: string;
            /** Format: date */
            fechaTela?: string;
            /** Format: date */
            fechaElaboracion?: string;
            /**
             * @description Marca de entregado a tienda.
             * @default false
             */
            entregadoTienda?: boolean;
            /**
             * @description Marcado para no producir.
             * @default false
             */
            noProducir?: boolean;
            /**
             * @description Renglones del pedido (modelo + cantidad + precio).
             * @default []
             */
            lineas?: {
              /** @description Id del renglón existente (solo en edición; ausente = renglón nuevo). */
              id?: number;
              /** @description Modelo pedido (del catálogo de modelos). */
              idModelo: number;
              /** @description Cantidad pedida del modelo (viejo: CantPed). */
              cantidadPedida: number;
              /** @description Precio pactado por prenda — snapshot del pedido (viejo: Precio). Opcional: un usuario sin `pedidos.importes` NO lo manda; el dominio conserva el precio almacenado (renglón existente) o usa 0 (renglón nuevo). */
              precio?: number;
            }[];
          };
        };
      };
      responses: {
        /** @description Pedido interno (encabezado + renglones). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno del pedido. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña del pedido y del folio. */
              idEmpresa: number;
              /** @description Cliente del pedido. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Fecha del pedido (YYYY-MM-DD), o null. */
              fechaPedido: string | null;
              /** @description Ventana de entrega — desde, o null. */
              fechaDe: string | null;
              /** @description Ventana de entrega — hasta, o null. */
              fechaHasta: string | null;
              /** @description Fecha de tela, o null. */
              fechaTela: string | null;
              /** @description Fecha de elaboración, o null. */
              fechaElaboracion: string | null;
              /** @description Marca de entregado a tienda. */
              entregadoTienda: boolean;
              /** @description Marcado para no producir. */
              noProducir: boolean;
              /** @description Cancelación suave: el pedido sigue consultable. */
              pedCancelado: boolean;
              /** @description Snapshot migrado de SOLO LECTURA: orden de compra ligada en el viejo (sin FK hasta F4). */
              idOrdCompraV1: number | null;
              /** @description Suma de las cantidades pedidas de los renglones. */
              totalPiezas: number;
              /** @description Suma de los importes (Σ cantidad × precio), o null si no puede ver importes. */
              totalImporte: number | null;
              /** @description Renglones del pedido. */
              lineas: {
                /** @description Id del renglón. */
                id: number;
                /** @description Id del modelo pedido. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description URL prefirmada de la foto principal del modelo, o null. */
                urlFotoModelo: string | null;
                /** @description Cantidad pedida. */
                cantidadPedida: number;
                /** @description Precio por prenda, o null si la sesión no puede ver importes. */
                precio: number | null;
                /** @description Importe del renglón (cantidad × precio), o null si no puede ver importes. */
                importe: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad ya entregada en el viejo (no saldo vivo). */
                entregadoParcialV1: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad faltante en el viejo (no saldo vivo). */
                cantFaltanteV1: number | null;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/pedidos/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un pedido interno */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Pedido interno (encabezado + renglones). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno del pedido. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña del pedido y del folio. */
              idEmpresa: number;
              /** @description Cliente del pedido. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Fecha del pedido (YYYY-MM-DD), o null. */
              fechaPedido: string | null;
              /** @description Ventana de entrega — desde, o null. */
              fechaDe: string | null;
              /** @description Ventana de entrega — hasta, o null. */
              fechaHasta: string | null;
              /** @description Fecha de tela, o null. */
              fechaTela: string | null;
              /** @description Fecha de elaboración, o null. */
              fechaElaboracion: string | null;
              /** @description Marca de entregado a tienda. */
              entregadoTienda: boolean;
              /** @description Marcado para no producir. */
              noProducir: boolean;
              /** @description Cancelación suave: el pedido sigue consultable. */
              pedCancelado: boolean;
              /** @description Snapshot migrado de SOLO LECTURA: orden de compra ligada en el viejo (sin FK hasta F4). */
              idOrdCompraV1: number | null;
              /** @description Suma de las cantidades pedidas de los renglones. */
              totalPiezas: number;
              /** @description Suma de los importes (Σ cantidad × precio), o null si no puede ver importes. */
              totalImporte: number | null;
              /** @description Renglones del pedido. */
              lineas: {
                /** @description Id del renglón. */
                id: number;
                /** @description Id del modelo pedido. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description URL prefirmada de la foto principal del modelo, o null. */
                urlFotoModelo: string | null;
                /** @description Cantidad pedida. */
                cantidadPedida: number;
                /** @description Precio por prenda, o null si la sesión no puede ver importes. */
                precio: number | null;
                /** @description Importe del renglón (cantidad × precio), o null si no puede ver importes. */
                importe: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad ya entregada en el viejo (no saldo vivo). */
                entregadoParcialV1: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad faltante en el viejo (no saldo vivo). */
                cantFaltanteV1: number | null;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    /** Actualizar un pedido interno */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Cliente del pedido (si se omite, no se toca). */
            idCliente?: number;
            fechaPedido?: string | null;
            fechaDe?: string | null;
            fechaHasta?: string | null;
            fechaTela?: string | null;
            fechaElaboracion?: string | null;
            /** @description Marca de entregado a tienda. */
            entregadoTienda?: boolean;
            /** @description Marcado para no producir. */
            noProducir?: boolean;
            /** @description Set COMPLETO de renglones (si se omite, no se tocan). */
            lineas?: {
              /** @description Id del renglón existente (solo en edición; ausente = renglón nuevo). */
              id?: number;
              /** @description Modelo pedido (del catálogo de modelos). */
              idModelo: number;
              /** @description Cantidad pedida del modelo (viejo: CantPed). */
              cantidadPedida: number;
              /** @description Precio pactado por prenda — snapshot del pedido (viejo: Precio). Opcional: un usuario sin `pedidos.importes` NO lo manda; el dominio conserva el precio almacenado (renglón existente) o usa 0 (renglón nuevo). */
              precio?: number;
            }[];
          };
        };
      };
      responses: {
        /** @description Pedido interno (encabezado + renglones). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno del pedido. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña del pedido y del folio. */
              idEmpresa: number;
              /** @description Cliente del pedido. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Fecha del pedido (YYYY-MM-DD), o null. */
              fechaPedido: string | null;
              /** @description Ventana de entrega — desde, o null. */
              fechaDe: string | null;
              /** @description Ventana de entrega — hasta, o null. */
              fechaHasta: string | null;
              /** @description Fecha de tela, o null. */
              fechaTela: string | null;
              /** @description Fecha de elaboración, o null. */
              fechaElaboracion: string | null;
              /** @description Marca de entregado a tienda. */
              entregadoTienda: boolean;
              /** @description Marcado para no producir. */
              noProducir: boolean;
              /** @description Cancelación suave: el pedido sigue consultable. */
              pedCancelado: boolean;
              /** @description Snapshot migrado de SOLO LECTURA: orden de compra ligada en el viejo (sin FK hasta F4). */
              idOrdCompraV1: number | null;
              /** @description Suma de las cantidades pedidas de los renglones. */
              totalPiezas: number;
              /** @description Suma de los importes (Σ cantidad × precio), o null si no puede ver importes. */
              totalImporte: number | null;
              /** @description Renglones del pedido. */
              lineas: {
                /** @description Id del renglón. */
                id: number;
                /** @description Id del modelo pedido. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description URL prefirmada de la foto principal del modelo, o null. */
                urlFotoModelo: string | null;
                /** @description Cantidad pedida. */
                cantidadPedida: number;
                /** @description Precio por prenda, o null si la sesión no puede ver importes. */
                precio: number | null;
                /** @description Importe del renglón (cantidad × precio), o null si no puede ver importes. */
                importe: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad ya entregada en el viejo (no saldo vivo). */
                entregadoParcialV1: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad faltante en el viejo (no saldo vivo). */
                cantFaltanteV1: number | null;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/pedidos/{id}/copiar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Copiar un pedido en uno nuevo (renglones seleccionados) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Ids de los renglones a copiar (vacío/omitido = todos). */
            idLineas?: number[];
          };
        };
      };
      responses: {
        /** @description Pedido interno (encabezado + renglones). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno del pedido. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña del pedido y del folio. */
              idEmpresa: number;
              /** @description Cliente del pedido. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Fecha del pedido (YYYY-MM-DD), o null. */
              fechaPedido: string | null;
              /** @description Ventana de entrega — desde, o null. */
              fechaDe: string | null;
              /** @description Ventana de entrega — hasta, o null. */
              fechaHasta: string | null;
              /** @description Fecha de tela, o null. */
              fechaTela: string | null;
              /** @description Fecha de elaboración, o null. */
              fechaElaboracion: string | null;
              /** @description Marca de entregado a tienda. */
              entregadoTienda: boolean;
              /** @description Marcado para no producir. */
              noProducir: boolean;
              /** @description Cancelación suave: el pedido sigue consultable. */
              pedCancelado: boolean;
              /** @description Snapshot migrado de SOLO LECTURA: orden de compra ligada en el viejo (sin FK hasta F4). */
              idOrdCompraV1: number | null;
              /** @description Suma de las cantidades pedidas de los renglones. */
              totalPiezas: number;
              /** @description Suma de los importes (Σ cantidad × precio), o null si no puede ver importes. */
              totalImporte: number | null;
              /** @description Renglones del pedido. */
              lineas: {
                /** @description Id del renglón. */
                id: number;
                /** @description Id del modelo pedido. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description URL prefirmada de la foto principal del modelo, o null. */
                urlFotoModelo: string | null;
                /** @description Cantidad pedida. */
                cantidadPedida: number;
                /** @description Precio por prenda, o null si la sesión no puede ver importes. */
                precio: number | null;
                /** @description Importe del renglón (cantidad × precio), o null si no puede ver importes. */
                importe: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad ya entregada en el viejo (no saldo vivo). */
                entregadoParcialV1: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad faltante en el viejo (no saldo vivo). */
                cantFaltanteV1: number | null;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/pedidos/{id}/cancelar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Cancelar un pedido (cancelación suave) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Pedido interno (encabezado + renglones). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno del pedido. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña del pedido y del folio. */
              idEmpresa: number;
              /** @description Cliente del pedido. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Fecha del pedido (YYYY-MM-DD), o null. */
              fechaPedido: string | null;
              /** @description Ventana de entrega — desde, o null. */
              fechaDe: string | null;
              /** @description Ventana de entrega — hasta, o null. */
              fechaHasta: string | null;
              /** @description Fecha de tela, o null. */
              fechaTela: string | null;
              /** @description Fecha de elaboración, o null. */
              fechaElaboracion: string | null;
              /** @description Marca de entregado a tienda. */
              entregadoTienda: boolean;
              /** @description Marcado para no producir. */
              noProducir: boolean;
              /** @description Cancelación suave: el pedido sigue consultable. */
              pedCancelado: boolean;
              /** @description Snapshot migrado de SOLO LECTURA: orden de compra ligada en el viejo (sin FK hasta F4). */
              idOrdCompraV1: number | null;
              /** @description Suma de las cantidades pedidas de los renglones. */
              totalPiezas: number;
              /** @description Suma de los importes (Σ cantidad × precio), o null si no puede ver importes. */
              totalImporte: number | null;
              /** @description Renglones del pedido. */
              lineas: {
                /** @description Id del renglón. */
                id: number;
                /** @description Id del modelo pedido. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description URL prefirmada de la foto principal del modelo, o null. */
                urlFotoModelo: string | null;
                /** @description Cantidad pedida. */
                cantidadPedida: number;
                /** @description Precio por prenda, o null si la sesión no puede ver importes. */
                precio: number | null;
                /** @description Importe del renglón (cantidad × precio), o null si no puede ver importes. */
                importe: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad ya entregada en el viejo (no saldo vivo). */
                entregadoParcialV1: number | null;
                /** @description Snapshot migrado de SOLO LECTURA: cantidad faltante en el viejo (no saldo vivo). */
                cantFaltanteV1: number | null;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/pedidos/{id}/reales': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar los pedidos reales de un pedido interno */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Pedidos reales de un pedido interno. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Pedidos reales del pedido interno. */
              datos: {
                /** @description Id del pedido real. */
                id: number;
                /** @description Pedido interno al que pertenece. */
                idPedido: number;
                /** @description Número del pedido real del cliente, o null. */
                numPedReal: string | null;
                /** @description CEDIS destino, o null. */
                cedis: string | null;
                /** @description Apertura/temporada, o null. */
                apertura: string | null;
                /** @description Fecha del pedido real, o null. */
                fechaPedPR: string | null;
                /** @description Inicio de la ventana de entrega, o null. */
                fechaInicio: string | null;
                /** @description Fin de la ventana de entrega, o null. */
                fechaFin: string | null;
                /** @description Fecha en que se entregó, o null. */
                fechaEntregadaReal: string | null;
                /** @description Renglones del pedido real. */
                lineas: {
                  /** @description Id del renglón del pedido real. */
                  id: number;
                  /** @description Renglón del pedido interno del que cuelga. */
                  idPedidoLinea: number;
                  /** @description Id del modelo (del renglón del pedido interno). */
                  idModelo: number;
                  /** @description Código del modelo (para la UI). */
                  codigoModelo: string;
                  /** @description Descripción del modelo, o null. */
                  descripcionModelo: string | null;
                  /** @description Cantidad pedida del renglón del pedido interno. */
                  cantidadPedida: number;
                  /** @description Precio del renglón interno, o null si no ve importes. */
                  precio: number | null;
                  /** @description Cantidad del pedido real. */
                  cantidadPR: number;
                  /** @description Cantidad enviada. */
                  cantidadEnviada: number;
                  /** @description Cantidad entregada/aceptada. */
                  cantidadEntregadaReal: number;
                  /** @description Empaques. */
                  empaques: number;
                }[];
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un pedido real desde un pedido interno */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Número del pedido real del cliente. */
            numPedReal?: string;
            /** @description Centro de distribución destino. */
            cedis?: string;
            /** @description Apertura/temporada (texto libre). */
            apertura?: string;
            /** Format: date */
            fechaPedPR?: string;
            /** Format: date */
            fechaInicio?: string;
            /** Format: date */
            fechaFin?: string;
          };
        };
      };
      responses: {
        /** @description Pedido real (liberación del cliente contra un pedido interno). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del pedido real. */
              id: number;
              /** @description Pedido interno al que pertenece. */
              idPedido: number;
              /** @description Número del pedido real del cliente, o null. */
              numPedReal: string | null;
              /** @description CEDIS destino, o null. */
              cedis: string | null;
              /** @description Apertura/temporada, o null. */
              apertura: string | null;
              /** @description Fecha del pedido real, o null. */
              fechaPedPR: string | null;
              /** @description Inicio de la ventana de entrega, o null. */
              fechaInicio: string | null;
              /** @description Fin de la ventana de entrega, o null. */
              fechaFin: string | null;
              /** @description Fecha en que se entregó, o null. */
              fechaEntregadaReal: string | null;
              /** @description Renglones del pedido real. */
              lineas: {
                /** @description Id del renglón del pedido real. */
                id: number;
                /** @description Renglón del pedido interno del que cuelga. */
                idPedidoLinea: number;
                /** @description Id del modelo (del renglón del pedido interno). */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description Cantidad pedida del renglón del pedido interno. */
                cantidadPedida: number;
                /** @description Precio del renglón interno, o null si no ve importes. */
                precio: number | null;
                /** @description Cantidad del pedido real. */
                cantidadPR: number;
                /** @description Cantidad enviada. */
                cantidadEnviada: number;
                /** @description Cantidad entregada/aceptada. */
                cantidadEntregadaReal: number;
                /** @description Empaques. */
                empaques: number;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/pedidos-reales/{idReal}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    /** Actualizar el encabezado de un pedido real */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido real. */
          idReal: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            numPedReal?: string | null;
            cedis?: string | null;
            apertura?: string | null;
            fechaPedPR?: string | null;
            fechaInicio?: string | null;
            fechaFin?: string | null;
            /** @description Fecha en que se entregó el pedido real, o null. */
            fechaEntregadaReal?: string | null;
          };
        };
      };
      responses: {
        /** @description Pedido real (liberación del cliente contra un pedido interno). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del pedido real. */
              id: number;
              /** @description Pedido interno al que pertenece. */
              idPedido: number;
              /** @description Número del pedido real del cliente, o null. */
              numPedReal: string | null;
              /** @description CEDIS destino, o null. */
              cedis: string | null;
              /** @description Apertura/temporada, o null. */
              apertura: string | null;
              /** @description Fecha del pedido real, o null. */
              fechaPedPR: string | null;
              /** @description Inicio de la ventana de entrega, o null. */
              fechaInicio: string | null;
              /** @description Fin de la ventana de entrega, o null. */
              fechaFin: string | null;
              /** @description Fecha en que se entregó, o null. */
              fechaEntregadaReal: string | null;
              /** @description Renglones del pedido real. */
              lineas: {
                /** @description Id del renglón del pedido real. */
                id: number;
                /** @description Renglón del pedido interno del que cuelga. */
                idPedidoLinea: number;
                /** @description Id del modelo (del renglón del pedido interno). */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description Cantidad pedida del renglón del pedido interno. */
                cantidadPedida: number;
                /** @description Precio del renglón interno, o null si no ve importes. */
                precio: number | null;
                /** @description Cantidad del pedido real. */
                cantidadPR: number;
                /** @description Cantidad enviada. */
                cantidadEnviada: number;
                /** @description Cantidad entregada/aceptada. */
                cantidadEntregadaReal: number;
                /** @description Empaques. */
                empaques: number;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/pedidos-reales/{idReal}/seguimiento': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    /** Capturar el seguimiento por renglón de un pedido real */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del pedido real. */
          idReal: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Renglones de seguimiento a actualizar (por id). */
            lineas: {
              /** @description Id del renglón del pedido real. */
              id: number;
              /** @description Cantidad del pedido real. */
              cantidadPR?: number;
              /** @description Cantidad enviada. */
              cantidadEnviada?: number;
              /** @description Cantidad realmente entregada/aceptada. */
              cantidadEntregadaReal?: number;
              /** @description Empaques. */
              empaques?: number;
            }[];
          };
        };
      };
      responses: {
        /** @description Pedido real (liberación del cliente contra un pedido interno). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del pedido real. */
              id: number;
              /** @description Pedido interno al que pertenece. */
              idPedido: number;
              /** @description Número del pedido real del cliente, o null. */
              numPedReal: string | null;
              /** @description CEDIS destino, o null. */
              cedis: string | null;
              /** @description Apertura/temporada, o null. */
              apertura: string | null;
              /** @description Fecha del pedido real, o null. */
              fechaPedPR: string | null;
              /** @description Inicio de la ventana de entrega, o null. */
              fechaInicio: string | null;
              /** @description Fin de la ventana de entrega, o null. */
              fechaFin: string | null;
              /** @description Fecha en que se entregó, o null. */
              fechaEntregadaReal: string | null;
              /** @description Renglones del pedido real. */
              lineas: {
                /** @description Id del renglón del pedido real. */
                id: number;
                /** @description Renglón del pedido interno del que cuelga. */
                idPedidoLinea: number;
                /** @description Id del modelo (del renglón del pedido interno). */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description Cantidad pedida del renglón del pedido interno. */
                cantidadPedida: number;
                /** @description Precio del renglón interno, o null si no ve importes. */
                precio: number | null;
                /** @description Cantidad del pedido real. */
                cantidadPR: number;
                /** @description Cantidad enviada. */
                cantidadEnviada: number;
                /** @description Cantidad entregada/aceptada. */
                cantidadEntregadaReal: number;
                /** @description Empaques. */
                empaques: number;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/ordenes': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar órdenes de producción */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página. */
          porPagina?: number;
          /** @description Texto a buscar (folio, código de modelo, cliente o valor de referencia). */
          busqueda?: string;
          /** @description Filtra por modelo. */
          idModelo?: number;
          /** @description Filtra por cliente. */
          idCliente?: number;
          /** @description Filtra por año de la fecha. */
          anio?: number;
          /** @description Filtra por estado. */
          estado?: 'capturada' | 'completa' | 'cancelada';
          /** @description Incluye las órdenes canceladas (cancelación suave). */
          incluirCanceladas?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'folio' | 'fecha' | 'fechaEntrega' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de órdenes. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Órdenes de la página. */
              datos: {
                /** @description Id interno de la orden. */
                id: number;
                /** @description Folio consecutivo por empresa. */
                folio: number;
                /** @description Empresa dueña de la orden y del folio. */
                idEmpresa: number;
                /**
                 * @description Estado DERIVADO de la orden (no editable).
                 * @enum {string}
                 */
                estado: 'capturada' | 'completa' | 'cancelada';
                /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
                idPedidoLinea: number | null;
                /** @description Modelo a producir. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description Cliente de la orden. */
                idCliente: number;
                /** @description Nombre del cliente (para la UI). */
                cliente: string;
                /** @description Maquilero asignado (Proveedor), o null. */
                idMaquilero: number | null;
                /** @description Nombre del maquilero, o null. */
                maquilero: string | null;
                /** @description Etiqueta de marca, o null. */
                idEtiquetaMarca: number | null;
                /** @description Nombre de la etiqueta de marca, o null. */
                etiquetaMarca: string | null;
                /** @description Tela dispuesta, o null. */
                idTela: number | null;
                /** @description Nombre de la tela, o null. */
                tela: string | null;
                /** @description Fecha de la orden (YYYY-MM-DD), o null. */
                fecha: string | null;
                /** @description Fecha de entrega comprometida, o null. */
                fechaEntrega: string | null;
                /** @description Observaciones generales, o null. */
                observaciones: string | null;
                /** @description Composición textil, o null. */
                composicion: string | null;
                /** @description La composición se capturó a mano. */
                compForzada: boolean;
                /** @description Observaciones de maquila, o null. */
                obsMaquila: string | null;
                /** @description No costear esta orden. */
                noCostear: boolean;
                /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
                fechaCompletada: string | null;
                /** @description Motivo de la cancelación, o null. */
                motivoCancelada: string | null;
                /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
                tallasV1: string | null;
                /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
                maquilaOrd: number | null;
                /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
                aplicacionOrd: number | null;
                /** @description Maquila pagada (dato de v1; motor en F6), o null. */
                pagada: boolean | null;
                /** @description Bandera RC de v1 (dato; motor en F5), o null. */
                enRiesgo: boolean | null;
                /** @description Bandera RC de v1 (dato; motor en F5), o null. */
                siRC: boolean | null;
                /** @description Bandera RC de v1 (dato; motor en F5), o null. */
                rcViva: boolean | null;
                /** @description Matriz: colores con sus tallas. */
                lineas: {
                  /** @description Id del renglón (color). */
                  id: number;
                  /** @description Id del color. */
                  idColor: number;
                  /** @description Nombre del color (para la UI). */
                  color: string;
                  /** @description Cantidades por talla. */
                  tallas: {
                    /** @description Id de la talla. */
                    idTalla: number;
                    /** @description Etiqueta de la talla (para la UI). */
                    etiquetaTalla: string;
                    /** @description Cantidad de prendas de esta talla. */
                    cantidad: number;
                  }[];
                  /** @description Suma de las cantidades de las tallas de este color. */
                  totalPiezas: number;
                }[];
                /** @description Total de prendas de la orden (Σ de todas las tallas). */
                totalPiezas: number;
                /** @description Valores de referencia del cliente. */
                referencias: {
                  /** @description Id del valor de referencia. */
                  id: number;
                  /** @description Campo de referencia del cliente. */
                  idClienteCampo: number;
                  /** @description Etiqueta del campo (para la UI). */
                  etiqueta: string;
                  /** @description Valor capturado. */
                  valor: string;
                }[];
                /** @description Comentarios (cronológicos). */
                comentarios: {
                  /** @description Id del comentario. */
                  id: number;
                  /** @description Usuario que lo escribió, o null. */
                  idUsuario: string | null;
                  /** @description Texto del comentario. */
                  comentario: string;
                  /**
                   * Format: date-time
                   * @description Fecha del comentario (ISO 8601).
                   */
                  fecha: string;
                }[];
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que la creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que la modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total de órdenes que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una orden de producción desde un renglón de pedido */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Renglón del pedido del que sale la orden (obligatorio; autorrellena modelo/cliente). */
            idPedidoLinea: number;
            /** @description Maquilero asignado (Proveedor con rol de maquila); en F2 solo asignación. */
            idMaquilero?: number | null;
            /** @description Etiqueta de marca de la orden. */
            idEtiquetaMarca?: number | null;
            /** @description Tela dispuesta para la orden. */
            idTela?: number | null;
            /** @description Fecha de la orden (YYYY-MM-DD). */
            fecha?: string | null;
            /** @description Fecha de entrega comprometida (YYYY-MM-DD). */
            fechaEntrega?: string | null;
            /** @description Observaciones generales. */
            observaciones?: string | null;
            /** @description Composición textil. */
            composicion?: string | null;
            /** @description La composición se capturó a mano (no derivó del BOM). */
            compForzada?: boolean;
            /** @description Observaciones de maquila. */
            obsMaquila?: string | null;
            /** @description No costear esta orden (viejo: NoCost). */
            noCostear?: boolean;
            /** @description Matriz inicial (colores con sus tallas/cantidades); opcional en el alta. */
            lineas?: {
              /** @description Id del renglón (color) existente (solo en edición; ausente = renglón nuevo). */
              id?: number;
              /** @description Color del catálogo (F1; en v1 era texto libre). */
              idColor: number;
              /**
               * @description Cantidades por talla de este color.
               * @default []
               */
              tallas?: {
                /** @description Talla del catálogo (D4). */
                idTalla: number;
                /** @description Cantidad de prendas de esta talla en este color (viejo: T1..T8). */
                cantidad: number;
              }[];
            }[];
          };
        };
      };
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener una orden de producción */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    /** Actualizar el encabezado de una orden */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Maquilero asignado (Proveedor con rol de maquila); en F2 solo asignación. */
            idMaquilero?: number | null;
            /** @description Etiqueta de marca de la orden. */
            idEtiquetaMarca?: number | null;
            /** @description Tela dispuesta para la orden. */
            idTela?: number | null;
            /** @description Fecha de la orden (YYYY-MM-DD). */
            fecha?: string | null;
            /** @description Fecha de entrega comprometida (YYYY-MM-DD). */
            fechaEntrega?: string | null;
            /** @description Observaciones generales. */
            observaciones?: string | null;
            /** @description Composición textil. */
            composicion?: string | null;
            /** @description La composición se capturó a mano (no derivó del BOM). */
            compForzada?: boolean;
            /** @description Observaciones de maquila. */
            obsMaquila?: string | null;
            /** @description No costear esta orden (viejo: NoCost). */
            noCostear?: boolean;
          };
        };
      };
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/ordenes/{id}/matriz': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    /** Guardar la matriz de una orden (colores y tallas) */
    put: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Set COMPLETO de renglones (colores con sus tallas). */
            lineas: {
              /** @description Id del renglón (color) existente (solo en edición; ausente = renglón nuevo). */
              id?: number;
              /** @description Color del catálogo (F1; en v1 era texto libre). */
              idColor: number;
              /**
               * @description Cantidades por talla de este color.
               * @default []
               */
              tallas?: {
                /** @description Talla del catálogo (D4). */
                idTalla: number;
                /** @description Cantidad de prendas de esta talla en este color (viejo: T1..T8). */
                cantidad: number;
              }[];
            }[];
          };
        };
      };
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/{id}/copiar-matriz': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Copiar la matriz de otra orden a esta */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Orden de la que se copia la matriz (de la misma empresa). */
            idOrdenOrigen: number;
          };
        };
      };
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/{id}/cancelar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Cancelar una orden (cancelación suave, motivo obligatorio) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Motivo de la cancelación (obligatorio; viejo: MotivoCancelada). */
            motivo: string;
          };
        };
      };
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/{id}/referencias': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    /** Guardar las referencias (campos de cliente) de una orden */
    put: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Set COMPLETO de valores de referencia (por campo del cliente). */
            referencias: {
              /** @description Campo de referencia del cliente (D7; debe ser del cliente de la orden y estar activo). */
              idClienteCampo: number;
              /** @description Valor capturado para ese campo en esta orden. */
              valor: string;
            }[];
          };
        };
      };
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/{id}/comentarios': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Agregar un comentario a una orden */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Texto del comentario (inmutable: no se edita ni se borra). */
            comentario: string;
          };
        };
      };
      responses: {
        /** @description Orden de producción (encabezado + matriz + referencias + comentarios). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id interno de la orden. */
              id: number;
              /** @description Folio consecutivo por empresa. */
              folio: number;
              /** @description Empresa dueña de la orden y del folio. */
              idEmpresa: number;
              /**
               * @description Estado DERIVADO de la orden (no editable).
               * @enum {string}
               */
              estado: 'capturada' | 'completa' | 'cancelada';
              /** @description Renglón de pedido de origen (null solo en órdenes históricas migradas). */
              idPedidoLinea: number | null;
              /** @description Modelo a producir. */
              idModelo: number;
              /** @description Código del modelo (para la UI). */
              codigoModelo: string;
              /** @description Descripción del modelo, o null. */
              descripcionModelo: string | null;
              /** @description Cliente de la orden. */
              idCliente: number;
              /** @description Nombre del cliente (para la UI). */
              cliente: string;
              /** @description Maquilero asignado (Proveedor), o null. */
              idMaquilero: number | null;
              /** @description Nombre del maquilero, o null. */
              maquilero: string | null;
              /** @description Etiqueta de marca, o null. */
              idEtiquetaMarca: number | null;
              /** @description Nombre de la etiqueta de marca, o null. */
              etiquetaMarca: string | null;
              /** @description Tela dispuesta, o null. */
              idTela: number | null;
              /** @description Nombre de la tela, o null. */
              tela: string | null;
              /** @description Fecha de la orden (YYYY-MM-DD), o null. */
              fecha: string | null;
              /** @description Fecha de entrega comprometida, o null. */
              fechaEntrega: string | null;
              /** @description Observaciones generales, o null. */
              observaciones: string | null;
              /** @description Composición textil, o null. */
              composicion: string | null;
              /** @description La composición se capturó a mano. */
              compForzada: boolean;
              /** @description Observaciones de maquila, o null. */
              obsMaquila: string | null;
              /** @description No costear esta orden. */
              noCostear: boolean;
              /** @description Fecha en que la orden quedó completa (sellada al primer guardado de matriz), o null. */
              fechaCompletada: string | null;
              /** @description Motivo de la cancelación, o null. */
              motivoCancelada: string | null;
              /** @description Cadena cruda de tallas del viejo, de SOLO LECTURA (trazabilidad). */
              tallasV1: string | null;
              /** @description Costo de maquila de v1 (dato; motor en F3/F6), o null. */
              maquilaOrd: number | null;
              /** @description Costo de aplicación/estampado de v1 (dato; motor en F3/F6), o null. */
              aplicacionOrd: number | null;
              /** @description Maquila pagada (dato de v1; motor en F6), o null. */
              pagada: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              enRiesgo: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              siRC: boolean | null;
              /** @description Bandera RC de v1 (dato; motor en F5), o null. */
              rcViva: boolean | null;
              /** @description Matriz: colores con sus tallas. */
              lineas: {
                /** @description Id del renglón (color). */
                id: number;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color (para la UI). */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta de la talla (para la UI). */
                  etiquetaTalla: string;
                  /** @description Cantidad de prendas de esta talla. */
                  cantidad: number;
                }[];
                /** @description Suma de las cantidades de las tallas de este color. */
                totalPiezas: number;
              }[];
              /** @description Total de prendas de la orden (Σ de todas las tallas). */
              totalPiezas: number;
              /** @description Valores de referencia del cliente. */
              referencias: {
                /** @description Id del valor de referencia. */
                id: number;
                /** @description Campo de referencia del cliente. */
                idClienteCampo: number;
                /** @description Etiqueta del campo (para la UI). */
                etiqueta: string;
                /** @description Valor capturado. */
                valor: string;
              }[];
              /** @description Comentarios (cronológicos). */
              comentarios: {
                /** @description Id del comentario. */
                id: number;
                /** @description Usuario que lo escribió, o null. */
                idUsuario: string | null;
                /** @description Texto del comentario. */
                comentario: string;
                /**
                 * Format: date-time
                 * @description Fecha del comentario (ISO 8601).
                 */
                fecha: string;
              }[];
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/consulta': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Consultar órdenes (listado ligero con filtros) */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página. */
          porPagina?: number;
          /** @description Texto a buscar (folio, código de modelo, cliente o valor de referencia). */
          busqueda?: string;
          /** @description Filtra por modelo. */
          idModelo?: number;
          /** @description Filtra por cliente. */
          idCliente?: number;
          /** @description Filtra por año de la fecha. */
          anio?: number;
          /** @description Filtra por estado. */
          estado?: 'capturada' | 'completa' | 'cancelada';
          /** @description Incluye las órdenes canceladas (cancelación suave). */
          incluirCanceladas?: string;
          /** @description Columna de orden. */
          ordenarPor?: 'folio' | 'fecha' | 'fechaEntrega' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de órdenes (proyección ligera). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Órdenes (ligeras) de la página. */
              datos: {
                /** @description Id interno de la orden. */
                id: number;
                /** @description Folio consecutivo por empresa. */
                folio: number;
                /**
                 * @description Estado DERIVADO de la orden (no editable).
                 * @enum {string}
                 */
                estado: 'capturada' | 'completa' | 'cancelada';
                /** @description Fecha de la orden (YYYY-MM-DD), o null. */
                fecha: string | null;
                /** @description Fecha de entrega comprometida, o null. */
                fechaEntrega: string | null;
                /** @description Modelo a producir. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description Cliente de la orden. */
                idCliente: number;
                /** @description Nombre del cliente (para la UI). */
                cliente: string;
                /** @description Maquilero asignado (Proveedor), o null. */
                idMaquilero: number | null;
                /** @description Nombre del maquilero, o null. */
                maquilero: string | null;
                /** @description Total de prendas de la orden (Σ de todas las tallas), agregado en servidor. */
                totalPiezas: number;
              }[];
              /** @description Total de órdenes que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/incompletas': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar órdenes incompletas (capturadas sin matriz) con semáforo de antigüedad */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página. */
          porPagina?: number;
          /** @description Dirección por antigüedad (desc = las más viejas/urgentes primero). */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de órdenes incompletas. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Órdenes incompletas de la página. */
              datos: {
                /** @description Id interno de la orden. */
                id: number;
                /** @description Folio consecutivo por empresa. */
                folio: number;
                /**
                 * @description Estado DERIVADO de la orden (no editable).
                 * @enum {string}
                 */
                estado: 'capturada' | 'completa' | 'cancelada';
                /** @description Fecha de la orden (YYYY-MM-DD), o null. */
                fecha: string | null;
                /** @description Fecha de entrega comprometida, o null. */
                fechaEntrega: string | null;
                /** @description Modelo a producir. */
                idModelo: number;
                /** @description Código del modelo (para la UI). */
                codigoModelo: string;
                /** @description Descripción del modelo, o null. */
                descripcionModelo: string | null;
                /** @description Cliente de la orden. */
                idCliente: number;
                /** @description Nombre del cliente (para la UI). */
                cliente: string;
                /** @description Maquilero asignado (Proveedor), o null. */
                idMaquilero: number | null;
                /** @description Nombre del maquilero, o null. */
                maquilero: string | null;
                /** @description Total de prendas de la orden (Σ de todas las tallas), agregado en servidor. */
                totalPiezas: number;
                /** @description Días desde el alta de la orden (creadoEn, o fecha como respaldo). */
                diasAntiguedad: number;
                /**
                 * @description Semáforo de antigüedad (verde ≤3d, amarillo 4–7d, urgente >7d). Derivado en servidor.
                 * @enum {string}
                 */
                semaforo: 'verde' | 'amarillo' | 'urgente';
              }[];
              /** @description Total de órdenes incompletas. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/tablero/pedidos-por-mes': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Tablero de pedidos por mes (agregado de órdenes) */
    get: {
      parameters: {
        query?: {
          /** @description Filtra por año (de la fecha de la orden). */
          anio?: number;
          /** @description Filtra por mes (1-12). */
          mes?: number;
          /** @description Filtra por cliente. */
          idCliente?: number;
          /** @description Incluye las órdenes canceladas en el agregado. */
          incluirCanceladas?: string;
          /** @description Bandera de paridad (viejo); sin efecto en F2 (no hay avance todavía). */
          entregadosTienda?: string;
          /** @description Bandera de paridad (viejo); sin efecto en F2 (no hay avance todavía). */
          noProducir?: string;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Tablero de pedidos por mes (agregado, extensible a avances en F3). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Una fila por mes (orden cronológico). */
              filas: {
                /** @description Año del mes agrupado. */
                anio: number;
                /** @description Mes (1-12). */
                mes: number;
                /** @description Clave estable del mes (YYYY-MM), para saltos/links. */
                clave: string;
                /** @description Etiqueta legible del mes (p. ej. "jun 2026"). */
                etiqueta: string;
                /** @description Número de órdenes con fecha en ese mes. */
                numOrdenes: number;
                /** @description Total de prendas (Σ de tallas) de ese mes. */
                totalPiezas: number;
              }[];
              /** @description Total de órdenes en todo el rango. */
              totalOrdenes: number;
              /** @description Total de prendas en todo el rango. */
              totalPiezas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/buscar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Buscador global de órdenes (hits ligeros para el layout) */
    get: {
      parameters: {
        query: {
          /** @description Texto a buscar (folio, código de modelo, cliente o valor de referencia D7). */
          q: string;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Resultados del buscador global de órdenes. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Hits de la búsqueda (limitados). */
              datos: {
                /** @description Id interno de la orden (para navegar al detalle). */
                id: number;
                /** @description Folio consecutivo por empresa. */
                folio: number;
                /** @description Código del modelo. */
                codigoModelo: string;
                /** @description Nombre del cliente. */
                cliente: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/{id}/impreso': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Imprimir una orden de producción (PDF de piso de producción) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la orden. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/ordenes/impresos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Imprimir varias órdenes en un solo PDF (una por página) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      /** @description Lote de órdenes a imprimir en un solo PDF. */
      requestBody: {
        content: {
          'application/json': {
            /** @description Ids de las órdenes a consolidar en el PDF (una por página). */
            ids: number[];
          };
        };
      };
      responses: {
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/tipos-proceso': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar tipos de proceso */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en el código o nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'codigo' | 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de tipos de proceso. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Tipos de proceso de la página. */
              datos: {
                /** @description Id del tipo de proceso. */
                id: number;
                /** @description Clave estable kebab-case (ej. "costura"). */
                codigo: string;
                /** @description Nombre para mostrar. */
                nombre: string;
                /** @description Si el recibo de este proceso genera entrada a inventario PT (decisión (e)). */
                generaEntradaPt: boolean;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo creó. */
                creadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Id del último usuario que lo modificó. */
                modificadoPorId: string | null;
              }[];
              /** @description Total que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un tipo de proceso */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            codigo: string;
            nombre: string;
            /** @description ¿El recibo de este proceso mete a inventario PT? Solo un admin puede fijarlo. */
            generaEntradaPt?: boolean;
          };
        };
      };
      responses: {
        /** @description Tipo de proceso de maquila (catálogo global). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del tipo de proceso. */
              id: number;
              /** @description Clave estable kebab-case (ej. "costura"). */
              codigo: string;
              /** @description Nombre para mostrar. */
              nombre: string;
              /** @description Si el recibo de este proceso genera entrada a inventario PT (decisión (e)). */
              generaEntradaPt: boolean;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/tipos-proceso/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un tipo de proceso */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del tipo de proceso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Tipo de proceso de maquila (catálogo global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del tipo de proceso. */
              id: number;
              /** @description Clave estable kebab-case (ej. "costura"). */
              codigo: string;
              /** @description Nombre para mostrar. */
              nombre: string;
              /** @description Si el recibo de este proceso genera entrada a inventario PT (decisión (e)). */
              generaEntradaPt: boolean;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un tipo de proceso (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del tipo de proceso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Tipo de proceso de maquila (catálogo global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del tipo de proceso. */
              id: number;
              /** @description Clave estable kebab-case (ej. "costura"). */
              codigo: string;
              /** @description Nombre para mostrar. */
              nombre: string;
              /** @description Si el recibo de este proceso genera entrada a inventario PT (decisión (e)). */
              generaEntradaPt: boolean;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un tipo de proceso */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del tipo de proceso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            codigo?: string;
            nombre?: string;
            /** @description ¿El recibo de este proceso mete a inventario PT? Solo un admin puede fijarlo. */
            generaEntradaPt?: boolean;
            activo?: boolean;
          };
        };
      };
      responses: {
        /** @description Tipo de proceso de maquila (catálogo global). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del tipo de proceso. */
              id: number;
              /** @description Clave estable kebab-case (ej. "costura"). */
              codigo: string;
              /** @description Nombre para mostrar. */
              nombre: string;
              /** @description Si el recibo de este proceso genera entrada a inventario PT (decisión (e)). */
              generaEntradaPt: boolean;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que lo creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que lo modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/tipos-movimiento': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar tipos de movimiento de inventario (solo lectura) */
    get: {
      parameters: {
        query?: {
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Filtra por dirección (entrada/salida/traspaso). */
          direccion?: 'entrada' | 'salida' | 'traspaso';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Lista de tipos de movimiento de inventario. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Tipos de movimiento. */
              datos: {
                /** @description Id del tipo de movimiento. */
                id: number;
                /** @description Clave estable kebab-case (ej. "entrada-maquila"). */
                codigo: string;
                /** @description Nombre para mostrar (ex IPT_TiposMov.TipoMov). */
                nombre: string;
                /**
                 * @description Dirección: entrada (+), salida (−) o traspaso.
                 * @enum {string}
                 */
                direccion: 'entrada' | 'salida' | 'traspaso';
                /** @description Falso si está desactivado. */
                activo: boolean;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/inventarios/pt/movimientos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Registrar un movimiento manual de inventario PT (entrada/salida/ajuste) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      /** @description Datos de un movimiento manual de inventario PT (color×talla, D4). */
      requestBody: {
        content: {
          'application/json': {
            /** @description Tipo de movimiento del catálogo (dirección entrada o salida; nunca traspaso). */
            idTipoMov: number;
            /** @description Almacén afectado (existencia por …×almacén, D4). */
            idAlmacen: number;
            /** @description Modelo del movimiento (un movimiento manual es de UN modelo). */
            idModelo: number;
            /**
             * Format: date
             * @description Fecha del movimiento (YYYY-MM-DD).
             */
            fecha: string;
            observaciones?: string;
            /** @description Matriz color×talla del movimiento (D4). */
            lineas: {
              idColor: number;
              /** @description Cantidades por talla de este color. */
              tallas: {
                idTalla: number;
                cantidad: number;
              }[];
            }[];
          };
        };
      };
      responses: {
        /** @description Movimiento de inventario PT con su matriz color×talla. */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del movimiento. */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Tipo de movimiento. */
              idTipoMov: number;
              /** @description Nombre del tipo de movimiento. */
              tipoMov: string;
              /**
               * @description Dirección del tipo.
               * @enum {string}
               */
              direccion: 'entrada' | 'salida' | 'traspaso';
              /** @description Almacén afectado. */
              idAlmacen: number;
              /** @description Nombre del almacén. */
              almacen: string;
              /** @description Modelo del movimiento. */
              idModelo: number;
              /** @description Código del modelo. */
              modelo: string;
              /** @description Fecha del movimiento (YYYY-MM-DD). */
              fecha: string;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Discriminador del hecho de origen o null. */
              origenTipo: string | null;
              /** @description Si el movimiento ya fue anulado por un inverso. */
              cancelado: boolean;
              /** @description Si este movimiento ES un inverso, el id del original que anula; si no, null. */
              idMovimientoInverso: number | null;
              /** @description Matriz color×talla del movimiento. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad de la talla. */
                  cantidad: number;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total de piezas del movimiento (derivado). */
              totalPiezas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que lo capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/inventarios/pt/traspasos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Traspasar PT entre almacenes (salida del origen + entrada al destino) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      /** @description Datos de un traspaso de PT entre almacenes (color×talla, D4). */
      requestBody: {
        content: {
          'application/json': {
            /** @description Almacén desde el que sale la mercancía. */
            idAlmacenOrigen: number;
            /** @description Almacén al que entra la mercancía (distinto del origen). */
            idAlmacenDestino: number;
            /** @description Modelo a traspasar (un traspaso es de UN modelo). */
            idModelo: number;
            /**
             * Format: date
             * @description Fecha del traspaso (YYYY-MM-DD).
             */
            fecha: string;
            observaciones?: string;
            /** @description Matriz color×talla del movimiento (D4). */
            lineas: {
              idColor: number;
              /** @description Cantidades por talla de este color. */
              tallas: {
                idTalla: number;
                cantidad: number;
              }[];
            }[];
          };
        };
      };
      responses: {
        /** @description Las dos patas de un traspaso de PT. */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Pata de SALIDA del almacén origen. */
              salida: {
                /** @description Id del movimiento. */
                id: number;
                /** @description Folio consecutivo por empresa (A3). */
                folio: number;
                /** @description Empresa dueña (A9). */
                idEmpresa: number;
                /** @description Tipo de movimiento. */
                idTipoMov: number;
                /** @description Nombre del tipo de movimiento. */
                tipoMov: string;
                /**
                 * @description Dirección del tipo.
                 * @enum {string}
                 */
                direccion: 'entrada' | 'salida' | 'traspaso';
                /** @description Almacén afectado. */
                idAlmacen: number;
                /** @description Nombre del almacén. */
                almacen: string;
                /** @description Modelo del movimiento. */
                idModelo: number;
                /** @description Código del modelo. */
                modelo: string;
                /** @description Fecha del movimiento (YYYY-MM-DD). */
                fecha: string;
                /** @description Observaciones o null. */
                observaciones: string | null;
                /** @description Discriminador del hecho de origen o null. */
                origenTipo: string | null;
                /** @description Si el movimiento ya fue anulado por un inverso. */
                cancelado: boolean;
                /** @description Si este movimiento ES un inverso, el id del original que anula; si no, null. */
                idMovimientoInverso: number | null;
                /** @description Matriz color×talla del movimiento. */
                lineas: {
                  /** @description Id del color. */
                  idColor: number;
                  /** @description Nombre del color. */
                  color: string;
                  /** @description Cantidades por talla. */
                  tallas: {
                    /** @description Id de la talla. */
                    idTalla: number;
                    /** @description Etiqueta visible de la talla. */
                    etiquetaTalla: string;
                    /** @description Cantidad de la talla. */
                    cantidad: number;
                  }[];
                  /** @description Total del renglón (derivado por suma). */
                  totalPiezas: number;
                }[];
                /** @description Total de piezas del movimiento (derivado). */
                totalPiezas: number;
                /**
                 * Format: date-time
                 * @description Fecha de captura (ISO).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo capturó. */
                creadoPorId: string | null;
              };
              /** @description Pata de ENTRADA al almacén destino. */
              entrada: {
                /** @description Id del movimiento. */
                id: number;
                /** @description Folio consecutivo por empresa (A3). */
                folio: number;
                /** @description Empresa dueña (A9). */
                idEmpresa: number;
                /** @description Tipo de movimiento. */
                idTipoMov: number;
                /** @description Nombre del tipo de movimiento. */
                tipoMov: string;
                /**
                 * @description Dirección del tipo.
                 * @enum {string}
                 */
                direccion: 'entrada' | 'salida' | 'traspaso';
                /** @description Almacén afectado. */
                idAlmacen: number;
                /** @description Nombre del almacén. */
                almacen: string;
                /** @description Modelo del movimiento. */
                idModelo: number;
                /** @description Código del modelo. */
                modelo: string;
                /** @description Fecha del movimiento (YYYY-MM-DD). */
                fecha: string;
                /** @description Observaciones o null. */
                observaciones: string | null;
                /** @description Discriminador del hecho de origen o null. */
                origenTipo: string | null;
                /** @description Si el movimiento ya fue anulado por un inverso. */
                cancelado: boolean;
                /** @description Si este movimiento ES un inverso, el id del original que anula; si no, null. */
                idMovimientoInverso: number | null;
                /** @description Matriz color×talla del movimiento. */
                lineas: {
                  /** @description Id del color. */
                  idColor: number;
                  /** @description Nombre del color. */
                  color: string;
                  /** @description Cantidades por talla. */
                  tallas: {
                    /** @description Id de la talla. */
                    idTalla: number;
                    /** @description Etiqueta visible de la talla. */
                    etiquetaTalla: string;
                    /** @description Cantidad de la talla. */
                    cantidad: number;
                  }[];
                  /** @description Total del renglón (derivado por suma). */
                  totalPiezas: number;
                }[];
                /** @description Total de piezas del movimiento (derivado). */
                totalPiezas: number;
                /**
                 * Format: date-time
                 * @description Fecha de captura (ISO).
                 */
                creadoEn: string;
                /** @description Id del usuario que lo capturó. */
                creadoPorId: string | null;
              };
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/inventarios/pt/movimientos/{id}/cancelar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Cancelar un movimiento PT (genera el inverso auditado; no edita ni borra) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Motivo de la cancelación del movimiento. */
      requestBody: {
        content: {
          'application/json': {
            motivo: string;
          };
        };
      };
      responses: {
        /** @description Movimiento de inventario PT con su matriz color×talla. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del movimiento. */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Tipo de movimiento. */
              idTipoMov: number;
              /** @description Nombre del tipo de movimiento. */
              tipoMov: string;
              /**
               * @description Dirección del tipo.
               * @enum {string}
               */
              direccion: 'entrada' | 'salida' | 'traspaso';
              /** @description Almacén afectado. */
              idAlmacen: number;
              /** @description Nombre del almacén. */
              almacen: string;
              /** @description Modelo del movimiento. */
              idModelo: number;
              /** @description Código del modelo. */
              modelo: string;
              /** @description Fecha del movimiento (YYYY-MM-DD). */
              fecha: string;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Discriminador del hecho de origen o null. */
              origenTipo: string | null;
              /** @description Si el movimiento ya fue anulado por un inverso. */
              cancelado: boolean;
              /** @description Si este movimiento ES un inverso, el id del original que anula; si no, null. */
              idMovimientoInverso: number | null;
              /** @description Matriz color×talla del movimiento. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad de la talla. */
                  cantidad: number;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total de piezas del movimiento (derivado). */
              totalPiezas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que lo capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/inventarios/pt/existencias': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Existencias de PT por modelo×color×talla×almacén (consulta) */
    get: {
      parameters: {
        query?: {
          /** @description Filtra por un modelo. */
          idModelo?: number;
          /** @description Filtra por un color. */
          idColor?: number;
          /** @description Filtra por una talla. */
          idTalla?: number;
          /** @description Filtra por un almacén. */
          idAlmacen?: number;
          /** @description Incluye filas con existencia 0 ("true"/"false"). Por defecto se omiten. */
          incluirCeros?: string;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Existencias de producto terminado (consulta de solo lectura, D3). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Existencias por modelo×color×talla×almacén. */
              filas: {
                /** @description Id del modelo. */
                idModelo: number;
                /** @description Código del modelo. */
                modelo: string;
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Id de la talla. */
                idTalla: number;
                /** @description Etiqueta visible de la talla. */
                etiquetaTalla: string;
                /** @description Orden del catálogo de la talla (para ordenar columnas). */
                ordenTalla: number;
                /** @description Id del almacén. */
                idAlmacen: number;
                /** @description Nombre del almacén. */
                almacen: string;
                /** @description Existencia actual (Σ de movimientos, D3). */
                existencia: number;
              }[];
              /** @description Suma de la existencia de todas las filas. */
              totalExistencia: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/inventarios/pt/kardex': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Kardex de un modelo (movimientos cronológicos con saldo corrido) */
    get: {
      parameters: {
        query: {
          /** @description Modelo del kardex (obligatorio). */
          idModelo: number;
          /** @description Filtra por un color. */
          idColor?: number;
          /** @description Filtra por una talla. */
          idTalla?: number;
          /** @description Filtra por un almacén. */
          idAlmacen?: number;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Kardex de un modelo (movimientos con saldo corrido). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Modelo del kardex. */
              idModelo: number;
              /** @description Código del modelo. */
              modelo: string;
              /** @description Movimientos en orden cronológico. */
              renglones: {
                /** @description Id del movimiento. */
                idMovimiento: number;
                /** @description Folio del movimiento. */
                folio: number;
                /** @description Fecha del movimiento (YYYY-MM-DD). */
                fecha: string;
                /** @description Tipo de movimiento. */
                idTipoMov: number;
                /** @description Nombre del tipo de movimiento. */
                tipoMov: string;
                /**
                 * @description Dirección del tipo.
                 * @enum {string}
                 */
                direccion: 'entrada' | 'salida' | 'traspaso';
                /** @description Almacén del movimiento. */
                idAlmacen: number;
                /** @description Nombre del almacén. */
                almacen: string;
                /** @description Color del renglón. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Talla del renglón. */
                idTalla: number;
                /** @description Etiqueta de la talla. */
                etiquetaTalla: string;
                /** @description Piezas que entran en este renglón (0 si es salida). */
                entrada: number;
                /** @description Piezas que salen en este renglón (0 si es entrada). */
                salida: number;
                /** @description Saldo corrido del artículo tras este movimiento. */
                saldo: number;
                /** @description Si el movimiento fue anulado por un inverso. */
                cancelado: boolean;
                /** @description Observaciones del movimiento o null. */
                observaciones: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/inventarios/pt/kardex/folio/{folio}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Detalle de un movimiento PT por su folio (empresa activa) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Folio del movimiento (consecutivo por empresa). */
          folio: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Movimiento de inventario PT con su matriz color×talla. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del movimiento. */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Tipo de movimiento. */
              idTipoMov: number;
              /** @description Nombre del tipo de movimiento. */
              tipoMov: string;
              /**
               * @description Dirección del tipo.
               * @enum {string}
               */
              direccion: 'entrada' | 'salida' | 'traspaso';
              /** @description Almacén afectado. */
              idAlmacen: number;
              /** @description Nombre del almacén. */
              almacen: string;
              /** @description Modelo del movimiento. */
              idModelo: number;
              /** @description Código del modelo. */
              modelo: string;
              /** @description Fecha del movimiento (YYYY-MM-DD). */
              fecha: string;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Discriminador del hecho de origen o null. */
              origenTipo: string | null;
              /** @description Si el movimiento ya fue anulado por un inverso. */
              cancelado: boolean;
              /** @description Si este movimiento ES un inverso, el id del original que anula; si no, null. */
              idMovimientoInverso: number | null;
              /** @description Matriz color×talla del movimiento. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad de la talla. */
                  cantidad: number;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total de piezas del movimiento (derivado). */
              totalPiezas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que lo capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/cortes': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Registrar un corte de una orden (color×talla; sobre-corte libre) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      /** @description Datos de un corte de orden (color×talla, D4; sobre-corte libre). */
      requestBody: {
        content: {
          'application/json': {
            idOrden: number;
            /** @description Proveedor con rol "corte". */
            idCortador: number;
            /**
             * Format: date
             * @description Fecha del corte (YYYY-MM-DD).
             */
            fecha: string;
            observaciones?: string;
            /** @description Matriz color×talla de la etapa (D4). */
            lineas: {
              idColor: number;
              /** @description Cantidades por talla de este color. */
              tallas: {
                idTalla: number;
                cantidad: number;
              }[];
            }[];
          };
        };
      };
      responses: {
        /** @description Etapa de producción (corte/envío) con su matriz color×talla. */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etapa. */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Orden a la que pertenece. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /**
               * @description Tipo de etapa.
               * @enum {string}
               */
              tipo: 'corte' | 'envio_maquila' | 'recibo_maquila' | 'entrega_cliente';
              /** @description Proceso de maquila (null en corte). */
              idTipoProceso: number | null;
              /** @description Nombre del proceso (null en corte). */
              tipoProceso: string | null;
              /** @description Cortador/maquilero (Proveedor). */
              idTercero: number | null;
              /** @description Nombre del cortador/maquilero. */
              tercero: string | null;
              /** @description Fecha de la etapa (YYYY-MM-DD). */
              fecha: string;
              /** @description Fecha compromiso (YYYY-MM-DD) o null. */
              fechaCompromiso: string | null;
              /** @description Precio pactado o null. */
              precioPactado: number | null;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Si la etapa está cancelada (suave). */
              cancelado: boolean;
              /** @description Cuándo se canceló (ISO) o null. */
              canceladoEn: string | null;
              /** @description Id del usuario que canceló o null. */
              canceladoPorId: string | null;
              /** @description Motivo de cancelación o null. */
              motivoCancelacion: string | null;
              /** @description Matriz color×talla de la etapa. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad de la talla. */
                  cantidad: number;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total de piezas de la etapa (derivado). */
              totalPiezas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que la capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/cortes/{id}/cancelar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Cancelar (suave) un corte */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Motivo de la cancelación de la etapa. */
      requestBody: {
        content: {
          'application/json': {
            motivo: string;
          };
        };
      };
      responses: {
        /** @description Etapa de producción (corte/envío) con su matriz color×talla. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etapa. */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Orden a la que pertenece. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /**
               * @description Tipo de etapa.
               * @enum {string}
               */
              tipo: 'corte' | 'envio_maquila' | 'recibo_maquila' | 'entrega_cliente';
              /** @description Proceso de maquila (null en corte). */
              idTipoProceso: number | null;
              /** @description Nombre del proceso (null en corte). */
              tipoProceso: string | null;
              /** @description Cortador/maquilero (Proveedor). */
              idTercero: number | null;
              /** @description Nombre del cortador/maquilero. */
              tercero: string | null;
              /** @description Fecha de la etapa (YYYY-MM-DD). */
              fecha: string;
              /** @description Fecha compromiso (YYYY-MM-DD) o null. */
              fechaCompromiso: string | null;
              /** @description Precio pactado o null. */
              precioPactado: number | null;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Si la etapa está cancelada (suave). */
              cancelado: boolean;
              /** @description Cuándo se canceló (ISO) o null. */
              canceladoEn: string | null;
              /** @description Id del usuario que canceló o null. */
              canceladoPorId: string | null;
              /** @description Motivo de cancelación o null. */
              motivoCancelacion: string | null;
              /** @description Matriz color×talla de la etapa. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad de la talla. */
                  cantidad: number;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total de piezas de la etapa (derivado). */
              totalPiezas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que la capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/envios': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Registrar un envío a maquila (costura/estampado/…; sobre-envío estricto) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      /** @description Datos de un envío a maquila (color×talla, D4; sobre-envío estricto). */
      requestBody: {
        content: {
          'application/json': {
            idOrden: number;
            /** @description Proceso de maquila (costura/estampado/…). */
            idTipoProceso: number;
            /** @description Proveedor con el rol que mapea al proceso. */
            idMaquilero: number;
            /**
             * Format: date
             * @description Fecha del envío (YYYY-MM-DD).
             */
            fecha: string;
            /** @description Fecha compromiso del maquilero (YYYY-MM-DD), opcional. */
            fechaCompromiso?: string | null;
            /** @description Precio de maquila pactado (base del cargo EsMa), opcional. */
            precioPactado?: number | null;
            observaciones?: string;
            /** @description Matriz color×talla de la etapa (D4). */
            lineas: {
              idColor: number;
              /** @description Cantidades por talla de este color. */
              tallas: {
                idTalla: number;
                cantidad: number;
              }[];
            }[];
          };
        };
      };
      responses: {
        /** @description Etapa de producción (corte/envío) con su matriz color×talla. */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etapa. */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Orden a la que pertenece. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /**
               * @description Tipo de etapa.
               * @enum {string}
               */
              tipo: 'corte' | 'envio_maquila' | 'recibo_maquila' | 'entrega_cliente';
              /** @description Proceso de maquila (null en corte). */
              idTipoProceso: number | null;
              /** @description Nombre del proceso (null en corte). */
              tipoProceso: string | null;
              /** @description Cortador/maquilero (Proveedor). */
              idTercero: number | null;
              /** @description Nombre del cortador/maquilero. */
              tercero: string | null;
              /** @description Fecha de la etapa (YYYY-MM-DD). */
              fecha: string;
              /** @description Fecha compromiso (YYYY-MM-DD) o null. */
              fechaCompromiso: string | null;
              /** @description Precio pactado o null. */
              precioPactado: number | null;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Si la etapa está cancelada (suave). */
              cancelado: boolean;
              /** @description Cuándo se canceló (ISO) o null. */
              canceladoEn: string | null;
              /** @description Id del usuario que canceló o null. */
              canceladoPorId: string | null;
              /** @description Motivo de cancelación o null. */
              motivoCancelacion: string | null;
              /** @description Matriz color×talla de la etapa. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad de la talla. */
                  cantidad: number;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total de piezas de la etapa (derivado). */
              totalPiezas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que la capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/envios/{id}/cancelar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Cancelar (suave) un envío a maquila */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Motivo de la cancelación de la etapa. */
      requestBody: {
        content: {
          'application/json': {
            motivo: string;
          };
        };
      };
      responses: {
        /** @description Etapa de producción (corte/envío) con su matriz color×talla. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la etapa. */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Orden a la que pertenece. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /**
               * @description Tipo de etapa.
               * @enum {string}
               */
              tipo: 'corte' | 'envio_maquila' | 'recibo_maquila' | 'entrega_cliente';
              /** @description Proceso de maquila (null en corte). */
              idTipoProceso: number | null;
              /** @description Nombre del proceso (null en corte). */
              tipoProceso: string | null;
              /** @description Cortador/maquilero (Proveedor). */
              idTercero: number | null;
              /** @description Nombre del cortador/maquilero. */
              tercero: string | null;
              /** @description Fecha de la etapa (YYYY-MM-DD). */
              fecha: string;
              /** @description Fecha compromiso (YYYY-MM-DD) o null. */
              fechaCompromiso: string | null;
              /** @description Precio pactado o null. */
              precioPactado: number | null;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Si la etapa está cancelada (suave). */
              cancelado: boolean;
              /** @description Cuándo se canceló (ISO) o null. */
              canceladoEn: string | null;
              /** @description Id del usuario que canceló o null. */
              canceladoPorId: string | null;
              /** @description Motivo de cancelación o null. */
              motivoCancelacion: string | null;
              /** @description Matriz color×talla de la etapa. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla. */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad de la talla. */
                  cantidad: number;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total de piezas de la etapa (derivado). */
              totalPiezas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que la capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/ordenes/{id}/pendientes': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Pendientes derivados de una orden (por cortar / cortado por enviar) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Pendientes derivados de una orden (corte y envío). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Orden. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /** @description orden − corte, por color×talla (negativo si hubo sobre-corte). */
              porCortar: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Id de la talla. */
                idTalla: number;
                /** @description Etiqueta visible de la talla. */
                etiquetaTalla: string;
                /** @description Cantidad pendiente (puede ser negativa por sobre-corte). */
                cantidad: number;
              }[];
              /** @description Total por cortar (derivado). */
              totalPorCortar: number;
              /** @description Total cortado de la orden (derivado). */
              cortadoTotal: number;
              /** @description cortado − enviado, por proceso ya usado en la orden. */
              cortadoPorEnviar: {
                /** @description Id del tipo de proceso. */
                idTipoProceso: number;
                /** @description Nombre del proceso. */
                tipoProceso: string;
                /** @description Código del proceso (kebab-case). */
                codigoProceso: string;
                /** @description cortado − enviado a este proceso, por color×talla. */
                celdas: {
                  /** @description Id del color. */
                  idColor: number;
                  /** @description Nombre del color. */
                  color: string;
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Cantidad pendiente (puede ser negativa por sobre-corte). */
                  cantidad: number;
                }[];
                /** @description Total pendiente por enviar a este proceso. */
                totalPendiente: number;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/ordenes/{id}/etapas': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Historial de etapas (cortes y envíos) de una orden, vivas y canceladas */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Historial de etapas (corte/envío) de una orden. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Orden. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /** @description Cortes y envíos de la orden (vivos y cancelados). */
              etapas: {
                /** @description Id de la etapa. */
                id: number;
                /** @description Folio consecutivo por empresa (A3). */
                folio: number;
                /** @description Empresa dueña (A9). */
                idEmpresa: number;
                /** @description Orden a la que pertenece. */
                idOrden: number;
                /** @description Folio de la orden. */
                folioOrden: number;
                /**
                 * @description Tipo de etapa.
                 * @enum {string}
                 */
                tipo: 'corte' | 'envio_maquila' | 'recibo_maquila' | 'entrega_cliente';
                /** @description Proceso de maquila (null en corte). */
                idTipoProceso: number | null;
                /** @description Nombre del proceso (null en corte). */
                tipoProceso: string | null;
                /** @description Cortador/maquilero (Proveedor). */
                idTercero: number | null;
                /** @description Nombre del cortador/maquilero. */
                tercero: string | null;
                /** @description Fecha de la etapa (YYYY-MM-DD). */
                fecha: string;
                /** @description Fecha compromiso (YYYY-MM-DD) o null. */
                fechaCompromiso: string | null;
                /** @description Precio pactado o null. */
                precioPactado: number | null;
                /** @description Observaciones o null. */
                observaciones: string | null;
                /** @description Si la etapa está cancelada (suave). */
                cancelado: boolean;
                /** @description Cuándo se canceló (ISO) o null. */
                canceladoEn: string | null;
                /** @description Id del usuario que canceló o null. */
                canceladoPorId: string | null;
                /** @description Motivo de cancelación o null. */
                motivoCancelacion: string | null;
                /** @description Matriz color×talla de la etapa. */
                lineas: {
                  /** @description Id del color. */
                  idColor: number;
                  /** @description Nombre del color. */
                  color: string;
                  /** @description Cantidades por talla. */
                  tallas: {
                    /** @description Id de la talla. */
                    idTalla: number;
                    /** @description Etiqueta visible de la talla. */
                    etiquetaTalla: string;
                    /** @description Cantidad de la talla. */
                    cantidad: number;
                  }[];
                  /** @description Total del renglón (derivado por suma). */
                  totalPiezas: number;
                }[];
                /** @description Total de piezas de la etapa (derivado). */
                totalPiezas: number;
                /**
                 * Format: date-time
                 * @description Fecha de captura (ISO).
                 */
                creadoEn: string;
                /** @description Id del usuario que la capturó. */
                creadoPorId: string | null;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/corte-semanal': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Corte semanal por cortador (consulta agrupada por cortador y semana) */
    get: {
      parameters: {
        query?: {
          /** @description Fecha inicial (YYYY-MM-DD), inclusiva. */
          desde?: string;
          /** @description Fecha final (YYYY-MM-DD), inclusiva. */
          hasta?: string;
          /** @description Filtra por un cortador concreto (Proveedor). */
          idCortador?: number;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Corte semanal por cortador. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Cortador × semana con su total cortado. */
              filas: {
                /** @description Cortador (Proveedor) o null si no se asignó. */
                idCortador: number | null;
                /** @description Nombre del cortador (o "Sin asignar"). */
                cortador: string;
                /** @description Año-semana ISO (p. ej. "2026-W25"). */
                anioSemana: string;
                /** @description Lunes de la semana (YYYY-MM-DD). */
                inicioSemana: string;
                /** @description Piezas cortadas (suma de las etapas vivas). */
                totalCortado: number;
                /** @description Número de cortes capturados esa semana. */
                numCortes: number;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/envios/{id}/impreso': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Documento de envío/entrega a maquila (PDF) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/envios/{id}/ficha-estampado': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Ficha de estampado que acompaña un envío de proceso (PDF) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/recibos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Registrar un recibo de maquila (WIP + entrada a PT en costura + cargo EsMa) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      /** @description Datos de un recibo de maquila (color×talla con calidad, D4). */
      requestBody: {
        content: {
          'application/json': {
            idOrden: number;
            /** @description Proceso de maquila que se recibe (costura/estampado/…). */
            idTipoProceso: number;
            /** @description Proveedor con el rol que mapea al proceso. */
            idMaquilero: number;
            /**
             * Format: date
             * @description Fecha del recibo (YYYY-MM-DD).
             */
            fecha: string;
            /** @description Liga opcional al envío que se recibe (mismo orden+proceso). */
            idEtapaEnvio?: number;
            /** @description Almacén destino de las primeras (solo si el proceso mete a PT — costura). */
            idAlmacenPrimeras?: number;
            /** @description Almacén destino de las segundas (solo si el proceso mete a PT — costura). */
            idAlmacenSegundas?: number;
            /** @description Precio de maquila (base del cargo EsMa). Opcional; suele heredarse del envío. */
            precioPactado?: number | null;
            observaciones?: string;
            /** @description Matriz color×talla del recibo (D4) con su calidad. */
            lineas: {
              idColor: number;
              /** @description Cantidades por talla de este color. */
              tallas: {
                idTalla: number;
                cantidad: number;
                /** @description Piezas de PRIMERA (buenas) de esta talla. Opcional; default = toda la cantidad. */
                cantidadPrimeras?: number;
                /** @description Piezas de SEGUNDA (defectuosas) de esta talla. Opcional; default 0. */
                cantidadSegundas?: number;
              }[];
            }[];
          };
        };
      };
      responses: {
        /** @description Recibo de maquila con su matriz color×talla y calidad. */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del recibo (EtapaMovimiento). */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Orden a la que pertenece. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /** @description Proceso de maquila. */
              idTipoProceso: number | null;
              /** @description Nombre del proceso. */
              tipoProceso: string | null;
              /** @description Si el proceso del recibo metió las primeras/segundas a inventario PT. */
              generaEntradaPt: boolean;
              /** @description Maquilero/estampador (Proveedor). */
              idTercero: number | null;
              /** @description Nombre del maquilero/estampador. */
              tercero: string | null;
              /** @description Envío ligado (opcional) o null. */
              idEtapaEnvio: number | null;
              /** @description Almacén destino de primeras o null. */
              idAlmacenPrimeras: number | null;
              /** @description Nombre del almacén de primeras o null. */
              almacenPrimeras: string | null;
              /** @description Almacén destino de segundas o null. */
              idAlmacenSegundas: number | null;
              /** @description Nombre del almacén de segundas o null. */
              almacenSegundas: string | null;
              /** @description Fecha del recibo (YYYY-MM-DD). */
              fecha: string;
              /** @description Precio pactado o null. */
              precioPactado: number | null;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Si el recibo está cancelado (suave). */
              cancelado: boolean;
              /** @description Cuándo se canceló (ISO) o null. */
              canceladoEn: string | null;
              /** @description Id del usuario que canceló o null. */
              canceladoPorId: string | null;
              /** @description Motivo de cancelación o null. */
              motivoCancelacion: string | null;
              /** @description PRIMER movimiento de kardex (entrada a PT) generado por el recibo de costura, o null si no metió a PT. Un recibo con primeras Y segundas genera DOS movimientos de entrada (uno por almacén); este campo expone solo el primero como indicador de "sí metió a PT". */
              idMovimientoEntrada: number | null;
              /** @description Matriz color×talla del recibo. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla (con calidad). */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Total recibido de la talla. */
                  cantidad: number;
                  /** @description Primeras (buenas) o null. */
                  cantidadPrimeras: number | null;
                  /** @description Segundas (defectuosas) o null. */
                  cantidadSegundas: number | null;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total recibido (derivado). */
              totalPiezas: number;
              /** @description Total de primeras (derivado). */
              totalPrimeras: number;
              /** @description Total de segundas (derivado). */
              totalSegundas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que lo capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/recibos/{id}/cancelar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Cancelar (suave + inverso de kardex si lo generó) un recibo de maquila */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Motivo de la cancelación del recibo. */
      requestBody: {
        content: {
          'application/json': {
            motivo: string;
          };
        };
      };
      responses: {
        /** @description Recibo de maquila con su matriz color×talla y calidad. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del recibo (EtapaMovimiento). */
              id: number;
              /** @description Folio consecutivo por empresa (A3). */
              folio: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Orden a la que pertenece. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /** @description Proceso de maquila. */
              idTipoProceso: number | null;
              /** @description Nombre del proceso. */
              tipoProceso: string | null;
              /** @description Si el proceso del recibo metió las primeras/segundas a inventario PT. */
              generaEntradaPt: boolean;
              /** @description Maquilero/estampador (Proveedor). */
              idTercero: number | null;
              /** @description Nombre del maquilero/estampador. */
              tercero: string | null;
              /** @description Envío ligado (opcional) o null. */
              idEtapaEnvio: number | null;
              /** @description Almacén destino de primeras o null. */
              idAlmacenPrimeras: number | null;
              /** @description Nombre del almacén de primeras o null. */
              almacenPrimeras: string | null;
              /** @description Almacén destino de segundas o null. */
              idAlmacenSegundas: number | null;
              /** @description Nombre del almacén de segundas o null. */
              almacenSegundas: string | null;
              /** @description Fecha del recibo (YYYY-MM-DD). */
              fecha: string;
              /** @description Precio pactado o null. */
              precioPactado: number | null;
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Si el recibo está cancelado (suave). */
              cancelado: boolean;
              /** @description Cuándo se canceló (ISO) o null. */
              canceladoEn: string | null;
              /** @description Id del usuario que canceló o null. */
              canceladoPorId: string | null;
              /** @description Motivo de cancelación o null. */
              motivoCancelacion: string | null;
              /** @description PRIMER movimiento de kardex (entrada a PT) generado por el recibo de costura, o null si no metió a PT. Un recibo con primeras Y segundas genera DOS movimientos de entrada (uno por almacén); este campo expone solo el primero como indicador de "sí metió a PT". */
              idMovimientoEntrada: number | null;
              /** @description Matriz color×talla del recibo. */
              lineas: {
                /** @description Id del color. */
                idColor: number;
                /** @description Nombre del color. */
                color: string;
                /** @description Cantidades por talla (con calidad). */
                tallas: {
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Total recibido de la talla. */
                  cantidad: number;
                  /** @description Primeras (buenas) o null. */
                  cantidadPrimeras: number | null;
                  /** @description Segundas (defectuosas) o null. */
                  cantidadSegundas: number | null;
                }[];
                /** @description Total del renglón (derivado por suma). */
                totalPiezas: number;
              }[];
              /** @description Total recibido (derivado). */
              totalPiezas: number;
              /** @description Total de primeras (derivado). */
              totalPrimeras: number;
              /** @description Total de segundas (derivado). */
              totalSegundas: number;
              /**
               * Format: date-time
               * @description Fecha de captura (ISO).
               */
              creadoEn: string;
              /** @description Id del usuario que lo capturó. */
              creadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/ordenes/{id}/pendientes-recibir': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Pendientes por recibir de una orden (enviado − recibido, por proceso) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Pendientes por recibir derivados de una orden (por proceso). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Orden. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /** @description enviado − recibido, por proceso ya usado en la orden. */
              porRecibir: {
                /** @description Id del tipo de proceso. */
                idTipoProceso: number;
                /** @description Nombre del proceso. */
                tipoProceso: string;
                /** @description Código del proceso (kebab-case). */
                codigoProceso: string;
                /** @description Si el recibo de este proceso mete a PT. */
                generaEntradaPt: boolean;
                /** @description enviado − recibido a este proceso, por color×talla (solo celdas ≠ 0). */
                celdas: {
                  /** @description Id del color. */
                  idColor: number;
                  /** @description Nombre del color. */
                  color: string;
                  /** @description Id de la talla. */
                  idTalla: number;
                  /** @description Etiqueta visible de la talla. */
                  etiquetaTalla: string;
                  /** @description Pendiente por recibir (enviado − recibido). */
                  cantidad: number;
                }[];
                /** @description Total pendiente por recibir de este proceso. */
                totalPendiente: number;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/recibos-semanales': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Recibos semanales por maquilero (consulta agrupada por maquilero y semana) */
    get: {
      parameters: {
        query?: {
          /** @description Fecha inicial (YYYY-MM-DD), inclusiva. */
          desde?: string;
          /** @description Fecha final (YYYY-MM-DD), inclusiva. */
          hasta?: string;
          /** @description Filtra por un maquilero concreto (Proveedor). */
          idMaquilero?: number;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Recibos semanales por maquilero. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Maquilero × semana con su total recibido. */
              filas: {
                /** @description Maquilero (Proveedor) o null. */
                idMaquilero: number | null;
                /** @description Nombre del maquilero (o "Sin asignar"). */
                maquilero: string;
                /** @description Año-semana ISO (p. ej. "2026-W25"). */
                anioSemana: string;
                /** @description Lunes de la semana (YYYY-MM-DD). */
                inicioSemana: string;
                /** @description Piezas recibidas (suma de los recibos vivos). */
                totalRecibido: number;
                /** @description Piezas de primera recibidas. */
                totalPrimeras: number;
                /** @description Piezas de segunda recibidas. */
                totalSegundas: number;
                /** @description Número de recibos capturados esa semana. */
                numRecibos: number;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/produccion/recibos/{id}/impreso': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Documento de recibo de maquila (PDF) */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del recurso. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/esma/cargos': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Cola de cargos EsMa por estado (default propuesto) */
    get: {
      parameters: {
        query?: {
          /** @description Estado del cargo a listar (default "propuesto"). */
          estado?: 'propuesto' | 'validado' | 'cancelado';
          /** @description Filtra por un maquilero concreto (Proveedor). */
          idMaquilero?: number;
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Cola de cargos EsMa. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Cargos EsMa del estado pedido. */
              filas: {
                /** @description Id del cargo. */
                id: number;
                /** @description Empresa dueña (A9). */
                idEmpresa: number;
                /** @description Recibo que originó el cargo o null. */
                idEtapaRecibo: number | null;
                /** @description Folio del recibo o null. */
                folioRecibo: number | null;
                /** @description Maquilero al que se carga (Proveedor). */
                idMaquilero: number;
                /** @description Nombre del maquilero. */
                maquilero: string;
                /** @description Orden a la que pertenece el cargo. */
                idOrden: number;
                /** @description Folio de la orden. */
                folioOrden: number;
                /** @description Proceso de maquila del cargo. */
                idTipoProceso: number;
                /** @description Nombre del proceso. */
                tipoProceso: string;
                /** @description Cantidad recibida que propuso el recibo (derivada del recibo). */
                cantidadPropuesta: number;
                /** @description Precio del envío propuesto (puede ser null si el envío no lo traía). */
                precioPropuesto: number | null;
                /** @description cantidadPropuesta × precioPropuesto, o null si no hay precio. */
                importePropuesto: number | null;
                /** @description Cantidad validada por el admin o null. */
                cantidadReal: number | null;
                /** @description Precio validado por el admin o null. */
                precioReal: number | null;
                /** @description cantidadReal × precioReal o null. */
                importeReal: number | null;
                /**
                 * @description Estado del cargo.
                 * @enum {string}
                 */
                estado: 'propuesto' | 'validado' | 'cancelado';
                /** @description Observaciones o null. */
                observaciones: string | null;
                /** @description Cuándo se validó (ISO) o null. */
                validadoEn: string | null;
                /** @description Id del usuario que validó o null. */
                validadoPorId: string | null;
                /**
                 * Format: date-time
                 * @description Cuándo se creó el cargo (ISO).
                 */
                creadoEn: string;
              }[];
              /** @description Suma de los importes propuestos (los que tienen precio). */
              totalImportePropuesto: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/esma/cargos/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un cargo EsMa */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cargo. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Cargo EsMa (cuenta de maquila) con su estado de validación. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del cargo. */
              id: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Recibo que originó el cargo o null. */
              idEtapaRecibo: number | null;
              /** @description Folio del recibo o null. */
              folioRecibo: number | null;
              /** @description Maquilero al que se carga (Proveedor). */
              idMaquilero: number;
              /** @description Nombre del maquilero. */
              maquilero: string;
              /** @description Orden a la que pertenece el cargo. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /** @description Proceso de maquila del cargo. */
              idTipoProceso: number;
              /** @description Nombre del proceso. */
              tipoProceso: string;
              /** @description Cantidad recibida que propuso el recibo (derivada del recibo). */
              cantidadPropuesta: number;
              /** @description Precio del envío propuesto (puede ser null si el envío no lo traía). */
              precioPropuesto: number | null;
              /** @description cantidadPropuesta × precioPropuesto, o null si no hay precio. */
              importePropuesto: number | null;
              /** @description Cantidad validada por el admin o null. */
              cantidadReal: number | null;
              /** @description Precio validado por el admin o null. */
              precioReal: number | null;
              /** @description cantidadReal × precioReal o null. */
              importeReal: number | null;
              /**
               * @description Estado del cargo.
               * @enum {string}
               */
              estado: 'propuesto' | 'validado' | 'cancelado';
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Cuándo se validó (ISO) o null. */
              validadoEn: string | null;
              /** @description Id del usuario que validó o null. */
              validadoPorId: string | null;
              /**
               * Format: date-time
               * @description Cuándo se creó el cargo (ISO).
               */
              creadoEn: string;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/esma/cargos/{id}/validar': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Validar (o ajustar cantidad y precio) un cargo EsMa propuesto */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del cargo. */
          id: number;
        };
        cookie?: never;
      };
      /** @description Datos de validación de un cargo EsMa (cantidad y precio reales). */
      requestBody: {
        content: {
          'application/json': {
            /** @description Cantidad real de piezas a pagar (la confirmada/ajustada por el admin). */
            cantidadReal: number;
            /** @description Precio unitario real de maquila (el confirmado/ajustado por el admin). */
            precioReal: number;
            observaciones?: string;
          };
        };
      };
      responses: {
        /** @description Cargo EsMa (cuenta de maquila) con su estado de validación. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del cargo. */
              id: number;
              /** @description Empresa dueña (A9). */
              idEmpresa: number;
              /** @description Recibo que originó el cargo o null. */
              idEtapaRecibo: number | null;
              /** @description Folio del recibo o null. */
              folioRecibo: number | null;
              /** @description Maquilero al que se carga (Proveedor). */
              idMaquilero: number;
              /** @description Nombre del maquilero. */
              maquilero: string;
              /** @description Orden a la que pertenece el cargo. */
              idOrden: number;
              /** @description Folio de la orden. */
              folioOrden: number;
              /** @description Proceso de maquila del cargo. */
              idTipoProceso: number;
              /** @description Nombre del proceso. */
              tipoProceso: string;
              /** @description Cantidad recibida que propuso el recibo (derivada del recibo). */
              cantidadPropuesta: number;
              /** @description Precio del envío propuesto (puede ser null si el envío no lo traía). */
              precioPropuesto: number | null;
              /** @description cantidadPropuesta × precioPropuesto, o null si no hay precio. */
              importePropuesto: number | null;
              /** @description Cantidad validada por el admin o null. */
              cantidadReal: number | null;
              /** @description Precio validado por el admin o null. */
              precioReal: number | null;
              /** @description cantidadReal × precioReal o null. */
              importeReal: number | null;
              /**
               * @description Estado del cargo.
               * @enum {string}
               */
              estado: 'propuesto' | 'validado' | 'cancelado';
              /** @description Observaciones o null. */
              observaciones: string | null;
              /** @description Cuándo se validó (ISO) o null. */
              validadoEn: string | null;
              /** @description Id del usuario que validó o null. */
              validadoPorId: string | null;
              /**
               * Format: date-time
               * @description Cuándo se creó el cargo (ISO).
               */
              creadoEn: string;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/usuarios': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar usuarios */
    get: {
      parameters: {
        query?: {
          /** @description Página (1-based). */
          pagina?: number;
          /** @description Renglones por página (máx 100). */
          porPagina?: number;
          /** @description Texto a buscar en usuario y nombre (insensible a mayúsculas). */
          busqueda?: string;
          /** @description Incluye los desactivados ("true"/"false"). */
          incluirInactivos?: string;
          /** @description Solo los bloqueados por intentos ("true"/"false"). */
          soloBloqueados?: string;
          /** @description Columna de ordenamiento. */
          ordenarPor?: 'username' | 'nombre' | 'creadoEn';
          /** @description Dirección del orden. */
          direccion?: 'asc' | 'desc';
        };
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Página de usuarios. */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Usuarios de la página. */
              datos: {
                /** @description Id del usuario (cuid). */
                id: string;
                /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
                username: string;
                /** @description Nombre completo de la persona. */
                nombre: string;
                /** @description Correo (puede ser sintético `<username>@control.local`). */
                email: string;
                /** @description Falso si está desactivado (borrado suave). */
                activo: boolean;
                /** @description Verdadero si quedó bloqueado por intentos fallidos. */
                bloqueado: boolean;
                /** @description Intentos de login fallidos acumulados. */
                intentosFallidos: number;
                /** @description Bandera de auditor de calidad. */
                esAuditor: boolean;
                /**
                 * Format: date-time
                 * @description Fecha de alta (ISO 8601).
                 */
                creadoEn: string;
                /**
                 * Format: date-time
                 * @description Fecha de la última modificación (ISO 8601).
                 */
                modificadoEn: string;
                /** @description Roles asignados al usuario (RBAC A4). */
                roles: {
                  /** @description Id del rol. */
                  id: number;
                  /** @description Nombre del rol. */
                  nombre: string;
                }[];
              }[];
              /** @description Total de usuarios que cumplen el filtro. */
              total: number;
              /** @description Página devuelta. */
              pagina: number;
              /** @description Renglones por página. */
              porPagina: number;
              /** @description Total de páginas. */
              totalPaginas: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear un usuario */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            username: string;
            nombre: string;
            /** Format: email */
            email?: string;
            password: string;
            /** @default false */
            esAuditor?: boolean;
            /** @default [] */
            idsRoles?: number[];
          };
        };
      };
      responses: {
        /** @description Usuario del sistema (sin datos de autenticación). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario (cuid). */
              id: string;
              /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
              username: string;
              /** @description Nombre completo de la persona. */
              nombre: string;
              /** @description Correo (puede ser sintético `<username>@control.local`). */
              email: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Verdadero si quedó bloqueado por intentos fallidos. */
              bloqueado: boolean;
              /** @description Intentos de login fallidos acumulados. */
              intentosFallidos: number;
              /** @description Bandera de auditor de calidad. */
              esAuditor: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Roles asignados al usuario (RBAC A4). */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Nombre del rol. */
                nombre: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/usuarios/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un usuario */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del usuario (cuid). */
          id: string;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Usuario del sistema (sin datos de autenticación). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario (cuid). */
              id: string;
              /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
              username: string;
              /** @description Nombre completo de la persona. */
              nombre: string;
              /** @description Correo (puede ser sintético `<username>@control.local`). */
              email: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Verdadero si quedó bloqueado por intentos fallidos. */
              bloqueado: boolean;
              /** @description Intentos de login fallidos acumulados. */
              intentosFallidos: number;
              /** @description Bandera de auditor de calidad. */
              esAuditor: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Roles asignados al usuario (RBAC A4). */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Nombre del rol. */
                nombre: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar un usuario (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del usuario (cuid). */
          id: string;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Usuario del sistema (sin datos de autenticación). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario (cuid). */
              id: string;
              /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
              username: string;
              /** @description Nombre completo de la persona. */
              nombre: string;
              /** @description Correo (puede ser sintético `<username>@control.local`). */
              email: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Verdadero si quedó bloqueado por intentos fallidos. */
              bloqueado: boolean;
              /** @description Intentos de login fallidos acumulados. */
              intentosFallidos: number;
              /** @description Bandera de auditor de calidad. */
              esAuditor: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Roles asignados al usuario (RBAC A4). */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Nombre del rol. */
                nombre: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar un usuario */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del usuario (cuid). */
          id: string;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            /** Format: email */
            email?: string;
            activo?: boolean;
            bloqueado?: boolean;
            esAuditor?: boolean;
            idsRoles?: number[];
          };
        };
      };
      responses: {
        /** @description Usuario del sistema (sin datos de autenticación). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario (cuid). */
              id: string;
              /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
              username: string;
              /** @description Nombre completo de la persona. */
              nombre: string;
              /** @description Correo (puede ser sintético `<username>@control.local`). */
              email: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Verdadero si quedó bloqueado por intentos fallidos. */
              bloqueado: boolean;
              /** @description Intentos de login fallidos acumulados. */
              intentosFallidos: number;
              /** @description Bandera de auditor de calidad. */
              esAuditor: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Roles asignados al usuario (RBAC A4). */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Nombre del rol. */
                nombre: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/usuarios/{id}/roles': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Reasignar los roles de un usuario */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del usuario (cuid). */
          id: string;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Ids de los roles que quedarán asignados (reemplazan a los actuales). */
            idsRoles: number[];
          };
        };
      };
      responses: {
        /** @description Usuario del sistema (sin datos de autenticación). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario (cuid). */
              id: string;
              /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
              username: string;
              /** @description Nombre completo de la persona. */
              nombre: string;
              /** @description Correo (puede ser sintético `<username>@control.local`). */
              email: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Verdadero si quedó bloqueado por intentos fallidos. */
              bloqueado: boolean;
              /** @description Intentos de login fallidos acumulados. */
              intentosFallidos: number;
              /** @description Bandera de auditor de calidad. */
              esAuditor: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Roles asignados al usuario (RBAC A4). */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Nombre del rol. */
                nombre: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/usuarios/{id}/desbloquear': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Desbloquear un usuario bloqueado por intentos */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del usuario (cuid). */
          id: string;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Usuario del sistema (sin datos de autenticación). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario (cuid). */
              id: string;
              /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
              username: string;
              /** @description Nombre completo de la persona. */
              nombre: string;
              /** @description Correo (puede ser sintético `<username>@control.local`). */
              email: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Verdadero si quedó bloqueado por intentos fallidos. */
              bloqueado: boolean;
              /** @description Intentos de login fallidos acumulados. */
              intentosFallidos: number;
              /** @description Bandera de auditor de calidad. */
              esAuditor: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Roles asignados al usuario (RBAC A4). */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Nombre del rol. */
                nombre: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/usuarios/{id}/contrasena': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /** Cambiar la contraseña de un usuario (reset por administrador) */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del usuario (cuid). */
          id: string;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Nueva contraseña (mín. 8 caracteres). */
            password: string;
          };
        };
      };
      responses: {
        /** @description Usuario del sistema (sin datos de autenticación). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del usuario (cuid). */
              id: string;
              /** @description Nombre de inicio de sesión (normalizado a minúsculas). */
              username: string;
              /** @description Nombre completo de la persona. */
              nombre: string;
              /** @description Correo (puede ser sintético `<username>@control.local`). */
              email: string;
              /** @description Falso si está desactivado (borrado suave). */
              activo: boolean;
              /** @description Verdadero si quedó bloqueado por intentos fallidos. */
              bloqueado: boolean;
              /** @description Intentos de login fallidos acumulados. */
              intentosFallidos: number;
              /** @description Bandera de auditor de calidad. */
              esAuditor: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Roles asignados al usuario (RBAC A4). */
              roles: {
                /** @description Id del rol. */
                id: number;
                /** @description Nombre del rol. */
                nombre: string;
              }[];
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/empresas': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar empresas */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la empresa. */
              id: number;
              /** @description Nombre corto de uso diario. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /** @description Identificador corto para folios, o null. */
              identificador: string | null;
              /** @description Empresa propuesta por defecto al iniciar sesión. */
              favorita: boolean;
              /** @description Participa en el inventario de producto terminado. */
              paraIpt: boolean;
              /** @description Participa en el estado de resultados. */
              paraEdr: boolean;
              /** @description Falso si está desactivada (borrado suave). */
              activa: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            }[];
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    /** Crear una empresa */
    post: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre: string;
            razonSocial?: string;
            identificador?: string;
            /** @default false */
            favorita?: boolean;
            /** @default false */
            paraIpt?: boolean;
            /** @default false */
            paraEdr?: boolean;
          };
        };
      };
      responses: {
        /** @description Empresa (multi-empresa explícito, A9). */
        201: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la empresa. */
              id: number;
              /** @description Nombre corto de uso diario. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /** @description Identificador corto para folios, o null. */
              identificador: string | null;
              /** @description Empresa propuesta por defecto al iniciar sesión. */
              favorita: boolean;
              /** @description Participa en el inventario de producto terminado. */
              paraIpt: boolean;
              /** @description Participa en el estado de resultados. */
              paraEdr: boolean;
              /** @description Falso si está desactivada (borrado suave). */
              activa: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/empresas/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener una empresa */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la empresa. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Empresa (multi-empresa explícito, A9). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la empresa. */
              id: number;
              /** @description Nombre corto de uso diario. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /** @description Identificador corto para folios, o null. */
              identificador: string | null;
              /** @description Empresa propuesta por defecto al iniciar sesión. */
              favorita: boolean;
              /** @description Participa en el inventario de producto terminado. */
              paraIpt: boolean;
              /** @description Participa en el estado de resultados. */
              paraEdr: boolean;
              /** @description Falso si está desactivada (borrado suave). */
              activa: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    /** Desactivar una empresa (borrado suave) */
    delete: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la empresa. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Empresa (multi-empresa explícito, A9). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la empresa. */
              id: number;
              /** @description Nombre corto de uso diario. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /** @description Identificador corto para folios, o null. */
              identificador: string | null;
              /** @description Empresa propuesta por defecto al iniciar sesión. */
              favorita: boolean;
              /** @description Participa en el inventario de producto terminado. */
              paraIpt: boolean;
              /** @description Participa en el estado de resultados. */
              paraEdr: boolean;
              /** @description Falso si está desactivada (borrado suave). */
              activa: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    options?: never;
    head?: never;
    /** Actualizar una empresa */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la empresa. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            nombre?: string;
            razonSocial?: string;
            identificador?: string;
            favorita?: boolean;
            paraIpt?: boolean;
            paraEdr?: boolean;
            activa?: boolean;
          };
        };
      };
      responses: {
        /** @description Empresa (multi-empresa explícito, A9). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la empresa. */
              id: number;
              /** @description Nombre corto de uso diario. */
              nombre: string;
              /** @description Razón social, o null. */
              razonSocial: string | null;
              /** @description Identificador corto para folios, o null. */
              identificador: string | null;
              /** @description Empresa propuesta por defecto al iniciar sesión. */
              favorita: boolean;
              /** @description Participa en el inventario de producto terminado. */
              paraIpt: boolean;
              /** @description Participa en el estado de resultados. */
              paraEdr: boolean;
              /** @description Falso si está desactivada (borrado suave). */
              activa: boolean;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /** @description Id del usuario que la creó. */
              creadoPorId: string | null;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
              /** @description Id del último usuario que la modificó. */
              modificadoPorId: string | null;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/empresas/{id}/configuracion': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener la configuración de una empresa */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la empresa. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Configuración por empresa (ex-Propiedades del sistema viejo). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la empresa dueña de la configuración. */
              idEmpresa: number;
              /** @description Utilidad sugerida (porcentaje), o null. */
              utilidadSugerida: number | null;
              /** @description Porcentaje base de regalías, o null. */
              regaliasBase: number | null;
              /** @description Días de colchón de costura, o null. */
              colchonCostura: number | null;
              /** @description Fecha del último inventario de telas (ISO 8601), o null. */
              fechaInventarioTelas: string | null;
              /** @description Fecha del último inventario de PT (ISO 8601), o null. */
              fechaInventarioPt: string | null;
              /** @description Almacén PT por defecto, o null. */
              idAlmacenPtDefault: number | null;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    /** Actualizar la configuración de una empresa */
    patch: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id de la empresa. */
          id: number;
        };
        cookie?: never;
      };
      requestBody: {
        content: {
          'application/json': {
            /** @description Utilidad sugerida para costeo (porcentaje). */
            utilidadSugerida?: number | null;
            /** @description Porcentaje base de regalías. */
            regaliasBase?: number | null;
            /** @description Días de colchón que la Ruta Crítica suma a la costura. */
            colchonCostura?: number | null;
            /** @description Fecha del último inventario físico de telas (ISO 8601). */
            fechaInventarioTelas?: string | null;
            /** @description Fecha del último inventario físico de PT (ISO 8601). */
            fechaInventarioPt?: string | null;
            /** @description Almacén PT por defecto (debe ser un almacén activo de tipo PT). */
            idAlmacenPtDefault?: number | null;
          };
        };
      };
      responses: {
        /** @description Configuración por empresa (ex-Propiedades del sistema viejo). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id de la empresa dueña de la configuración. */
              idEmpresa: number;
              /** @description Utilidad sugerida (porcentaje), o null. */
              utilidadSugerida: number | null;
              /** @description Porcentaje base de regalías, o null. */
              regaliasBase: number | null;
              /** @description Días de colchón de costura, o null. */
              colchonCostura: number | null;
              /** @description Fecha del último inventario de telas (ISO 8601), o null. */
              fechaInventarioTelas: string | null;
              /** @description Fecha del último inventario de PT (ISO 8601), o null. */
              fechaInventarioPt: string | null;
              /** @description Almacén PT por defecto, o null. */
              idAlmacenPtDefault: number | null;
              /**
               * Format: date-time
               * @description Fecha de alta (ISO 8601).
               */
              creadoEn: string;
              /**
               * Format: date-time
               * @description Fecha de la última modificación (ISO 8601).
               */
              modificadoEn: string;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    trace?: never;
  };
  '/api/roles': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Listar roles */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path?: never;
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Default Response */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del rol. */
              id: number;
              /** @description Nombre del rol. */
              nombre: string;
              /** @description Descripción del rol. */
              descripcion: string;
              /** @description Verdadero si es un rol de sistema (sembrado; no se renombra ni se borra). */
              esSistema: boolean;
              /** @description Claves de permiso (`modulo.accion`) que otorga el rol. */
              clavesPermisos: string[];
              /** @description Cuántos usuarios tienen este rol. */
              totalUsuarios: number;
            }[];
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  '/api/roles/{id}': {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /** Obtener un rol */
    get: {
      parameters: {
        query?: never;
        header?: never;
        path: {
          /** @description Id del rol. */
          id: number;
        };
        cookie?: never;
      };
      requestBody?: never;
      responses: {
        /** @description Rol del sistema con sus permisos (RBAC A4). */
        200: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Id del rol. */
              id: number;
              /** @description Nombre del rol. */
              nombre: string;
              /** @description Descripción del rol. */
              descripcion: string;
              /** @description Verdadero si es un rol de sistema (sembrado; no se renombra ni se borra). */
              esSistema: boolean;
              /** @description Claves de permiso (`modulo.accion`) que otorga el rol. */
              clavesPermisos: string[];
              /** @description Cuántos usuarios tienen este rol. */
              totalUsuarios: number;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        400: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        401: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        403: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        404: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
        /** @description Respuesta de error de la API. */
        409: {
          headers: {
            [name: string]: unknown;
          };
          content: {
            'application/json': {
              /** @description Código estable del error (p. ej. VALIDACION, PERMISO, NO_AUTENTICADO). */
              codigo: string;
              /** @description Mensaje en español, apto para mostrar al usuario. */
              mensaje: string;
              /** @description Detalle estructurado opcional (p. ej. errores por campo). */
              detalles?: unknown;
            };
          };
        };
      };
    };
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
}
export type webhooks = Record<string, never>;
export interface components {
  schemas: never;
  responses: never;
  parameters: never;
  requestBodies: never;
  headers: never;
  pathItems: never;
}
export type $defs = Record<string, never>;
export type operations = Record<string, never>;

{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Renovate de CONTROL v2 (PLANMAESTRO.md §1, regla de versiones: lockfiles al dia con PRs revisadas como cualquier otra). Detecta los dos servicios npm autonomos (backend/ y frontend/) por sus package-lock.json. Vive en .github/ porque Renovate solo lee su config en la raiz del repo o en .github/.",
  "extends": ["config:recommended"],
  "timezone": "America/Mexico_City",
  "labels": ["dependencias"],
  "rangeStrategy": "pin",
  "baseBranches": ["prueba"],
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 6am on monday"]
  },
  "packageRules": [
    {
      "description": "Minor y patch agrupados en un solo PR semanal",
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "dependencias menores (minor y patch)",
      "schedule": ["before 6am on monday"]
    },
    {
      "description": "Majors: un PR individual por dependencia, revisado con calma",
      "matchUpdateTypes": ["major"],
      "addLabels": ["dependencia-major"],
      "schedule": ["before 6am on monday"]
    }
  ]
}
